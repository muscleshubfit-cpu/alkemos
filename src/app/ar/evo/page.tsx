/**
 * SEO-GEO-6.4 (2026-09-12): /ar/evo — Arabic mirror (P1-9, §12.19).
 * Same bilingual client page; the root layout resolves AR from the
 * x-pathname header so /ar/evo server-renders Arabic (isAr branches
 * already exist throughout the page).
 */
export { default } from "@/app/evo/page";
