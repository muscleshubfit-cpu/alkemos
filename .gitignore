# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.local
.env.*.local

# duplicate lockfile — we use bun.lock (Vercel runs `bun install`)
package-lock.json
yarn.lock
pnpm-lock.yaml

# vercel
.vercel

# typescript
*.tsbuildinfo

# scripts logs
*.log

# Original cloned repo (kept locally for reference, NOT pushed to GitHub)
/repo/
/tool-results/
# Everything under /scripts is local tooling/logs EXCEPT the GHA
# runners + CI guard, which MUST be versioned (workflows depend on them):
#   scripts/blog-runner        → blog pipeline v2 (generate-blog-post.yml)
#   scripts/ai-jobs-runner     → whole-AI-system queue worker (process-ai-jobs.yml)
#   scripts/check-stale-refs.sh → anti-regression guard (guard-stale-refs.yml)
#   scripts/migration_audit.py → migrations ↔ types.ts audit (docs-parity-gate.yml)
#   scripts/docs_parity.py     → migration-registry gate (docs-parity-gate.yml, Phase 106)
#   scripts/docs_audit.py      → knowledge-system gate (docs-parity-gate.yml, Phase 107)
#   scripts/db-backup.mjs      → daily DB snapshot (db-backup.yml, 2026-09-05)
#   scripts/db-restore.mjs     → snapshot restore tool (db-backup.yml docs)
#   scripts/retro-pair-blog.mjs → Proposal-3 retro pairing applier (retro-pair-blog.yml, 2026-09-09)
#   scripts/retro-pair-blog.pairs.json → approved pairs + owner review list (retro-pair-blog.yml)
#   scripts/legacy-msa-patches/ → hand-written MSA conversions for the chain-unconvertible stubborn AR articles (legacy-ar-cleanup.yml PATCH mode, 2026-09-11)
#   scripts/vercel-cleanup/    → daily Vercel stale-deployment purge (vercel-cleanup.yml, Phase 145)
#   scripts/generate-og-cards.py → og:image family-card generator (Phase 188 — guarded by og-image-coverage.test.ts; the file was never committed because the /scripts/* blanket swallowed it, so the guard failed in CI from f954e85 until Phase 189 fixed the exception list)
#   scripts/cls-measure.mjs    → lab CLS measurement with throttling + per-shift attribution (Phase 216 P2-5 — the verification tool for the Arabic-fallback fix)
#   scripts/js-transfer-measure.mjs → critical-vs-post-load JS transfer measurement (Phase 216 P2-4 — the calibration evidence tool)
#   scripts/storage-inventory.mjs → daily storage bucket census (storage-inventory.yml, P0-3(b) hardening plan 2026-09-20 — hit the same /scripts/* blanket trap as generate-og-cards.py; exception added same frame)
#   scripts/external-mirror/ → both-repos external git mirror (external-mirror.yml + docs/RECOVERY-MIRROR-SETUP.md, P0-1 hardening plan 2026-09-20 — dormant until owner setup)
#   scripts/storage-backup.mjs → daily Supabase Storage FILE backup (storage-backup.yml, P0-3(c) owner order 2026-09-20 — same /scripts/* blanket trap; exception added same frame)
#   scripts/storage-restore.mjs → storage restore/drill tool (storage-restore-drill.yml + runbook §5, P0-3(c) 2026-09-20)
#   scripts/generate-search-indexes.ts → EVO search light-index generator (VERCEL-USAGE-3, 2026-09-21 — regenerated when foods.ts/exercises.ts change)
/scripts/*
!/scripts/blog-runner/
!/scripts/ai-jobs-runner/
!/scripts/vercel-cleanup/
!/scripts/legacy-msa-patches/
!/scripts/check-stale-refs.sh
!/scripts/migration_audit.py
!/scripts/docs_parity.py
!/scripts/docs_audit.py
!/scripts/db-backup.mjs
!/scripts/db-restore.mjs
!/scripts/retro-pair-blog.mjs
!/scripts/retro-pair-blog.pairs.json
!/scripts/generate-og-cards.py
!/scripts/cls-measure.mjs
!/scripts/js-transfer-measure.mjs
!/scripts/storage-inventory.mjs
!/scripts/storage-backup.mjs
!/scripts/storage-restore.mjs
!/scripts/generate-search-indexes.ts
!/scripts/external-mirror/
/skills/
/tests/
/examples/
/.zscripts/
/dev.log
/server.log
/download/
/prisma/
/mini-services/

# Database artifacts
/db/
*.db
.env*
.z-ai-config
.env.production

# Junk files (screenshots, uploads, temp images)
/upload/
/Screenshot_*.jpg
/Screenshot_*.png
/file_*.png
/.claude/
/Caddyfile
en_logs/
en_logs.zip
*_logs/
*_logs.zip
ar_logs/
ar_logs.zip
ar2_logs/
ar2_logs.zip

# Local live-verification screenshots (worklog: لقطات محلية فقط)
/testing/
