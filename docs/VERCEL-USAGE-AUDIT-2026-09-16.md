# تدقيق وتنظيف استهلاك Vercel — VERCEL-USAGE-1 (2026-09-16)

> **الحالة:** منفَّذ بأمر المالك المباشر («مطلوب فحص و تنظيف استهلاك vercel» — IM 2026-09-16). هذا الفريم مستقل عن موجات خطة التدقيق العميق (WAVE 2/3) ولا يغيّر ترقيمها.
> **الخطة الحالية:** Hobby (دليل run-step.mts «respect the Vercel Hobby limit» + حارس حصة الصور المرحلة 97 في next.config.ts).
> **المرجع الحاكم:** AGENTS.md — البوابات §3.5 قبل الدفع. توصيات لوحة التحكم أدناه **بقرار المالك** وفق §7 (تغيير لوحة Cloudflare يتبع قانون §5.4 في TECH_REFERENCE).

---

## 1. الخلاصة التنفيذية

الفحص الشامل للمستودع (كل مسارات API الـ76 + middleware + next.config/vercel.json + workflows + الأصول العامة + سلوك كاش Cloudflare الموثق) خلّص إلى ثلاث حقائق:

1. **البنية التحتية محسّنة أصلًا في أغلبها:** خط أنابيب المدونة يعمل داخل GitHub Actions (صفر وقت دوال على Vercel)، الصور بلا حصة تحسين (`unoptimized`) ومستضافة ذاتيًا بكاش غير قابل للتلف، خرائط الموقع/RSS/llms بـ ISR ساعة، وrobots يحجب `/api/` عن الزواحف، و`NotificationBell` يتوقف عند إخفاء التبويب.
2. **ثلاث فجوات استنزاف حقيقية أُغلقت بالكود في هذا الفريم:** مسار ميت يحرق استدعاءات غير موثقة (`/api/exercise-image`)، وأربع أسطح تستقصي API في التبويبات الخلفية طوال اليوم، و…
3. **الرافعة الأكبر المتبقية خارج الكود:** `/api/og-image/[slug]` مستثنى من قاعدة كاش CF لأنه يبدأ بـ`api/` → كل جلب بطاقة من زواحف السوشيال = دالة Edge + استعلام Supabase + توليد Satori + نقل PNG كامل عبر Vercel. **الحل قرار لوحة تحكم (توصية ت-1 أدناه)** — لم يُنقل المسار في الكود عمدًا لأن عشرات الاختبارات تثبّت صيغة URL المفهرسة أصلاً لدى المنصات.

## 2. ما نُفِّذ في هذا الفريم (تنظيف بالكود)

| # | التغيير | الأثر على الاستهلاك |
|---|---|---|
| ن-1 | **حذف `/api/exercise-image` بالكامل** + مخططه `exerciseNameSchema` + كتلة اختباره + نمط `wger.de` الميت في next.config | كان مسارًا ميتًا (صفر مستدعين منذ استضافة صور التمارين ذاتيًا في الدفعة 2 §12.53-2): كل طلب = استدعاء دالة غير موثق + طلبان صادران لـ wger.de لكل اسم بارد. **يُسقط نصف بند P3-11 من خطة التدقيق** (الحذف أنظف من الـ rate-limit المقترح هناك — نفس فلسفة guard-stale-refs) |
| ن-2 | **إيقاف الاستقصاء عند إخفاء التبويب** في `AdminNotificationBell` (30ث) + `CoachSupportView` (20ث + مستقصي التذكرة المفتوحة 10ث) + `AdminCoachSupportView` (30ث) + `CoachHelpView` (30ث) — بنفس نمط `NotificationBell` المعتمد | كل تبويب خلفي كان يحرق استدعاء API + middleware (بما فيها قفزة `getUser()` لكل مستخدم موثق) كل 10–30 ثانية إلى ما لا نهاية. الآن: صفر استدعاءات وهو مخفي، واستئناف فوري عند العودة |

**نطاق الأمان:** كل التعديلات سلوك-محايدة للوظيفة المرئية (jsdom في الاختبارات يرى التبويب مرئيًا افتراضيًا فلا تتأثر الحرّاس)، والمسار المحذوف بلا أي مستدعٍ حي (تأكد rg شامل + حرّاس guard-stale-refs).

## 3. خريطة الاستهلاك الحالية (مرتبة بالأثر)

| # | المصدر | آلية الاستهلاك | الوضع |
|---|---|---|---|
| 1 | **`/api/og-image/[slug]`** (edge + Supabase + Satori، PNG 25–250KB) | خارج قاعدة كاش CF (مسار `api/`) → كل جلب من WhatsApp/Telegram/Facebook/X/زواحف AI = تشغيل كامل عند الأصل | **بانتظار قرار المالك (ت-1)** |
| 2 | **رندر SSR الديناميكي لكل الصفحات** (قانون اللغة: root layout يقرأ `headers()/cookies()`) | كل انتهاء مدخل حافة CF (3600ث لكل PoP لكل URL) = دالة + رندر كامل. مع ~2,160 صفحة وعدد PoPs، هذا أكبر مولّد لـ GB-hours ونقل البيانات عند الـ miss | بنيوي (إصلاحه = إعادة هيكلة `[lang]` ضخمة خارج نطاق التنظيف) — **التخفيف العملي = ت-3** |
| 3 | **csp-report** (POST لكل مخالفة Report-Only من متصفح كل زائر) | استدعاء nodejs + middleware لكل مخالفة؛ الحجم غير معروف بدون سجلات | **يبقى عمدًا** — خطة التدقيق (م1) تعتمد على تقاريره لتفعيل CSP الكامل؛ يُعاد تقييمه بعد تفعيل م1 |
| 4 | **دردشة EVO + أدوات AI** (streaming عبر دوال Vercel) | مدة الدالة = مدة التوليد. عمل أساسي مدفوع الثمن | محمي (لا يُلمس) |
| 5 | **`getUser()` في middleware لكل طلب موثق** | قفزة شبكة Supabase لكل طلب (تضخيم مدة الدالة) | **ت-4 (بقرار المالك)** |
| 6 | **Speed Insights + Web Analytics beacons** | منتجات مقيسة (2,500 حدث مجاني على Hobby ثم تتوقف/تُفوتر حسب وجود وسيلة دفع) | **ت-2 (بقرار المالك)** |

**ما هو محسّن ولا يحتاج شيئًا (تحقق هذا الفريم):** خط المدونة كله على GitHub Actions in-process («NO Vercel hop» في run-step.mts) · مسارات `/api/cron/blog/*` احتياطية فقط · الصور `unoptimized` + `/images/exercises/` بكاش immutable سنة (CF يكاشها بترويستها) · sitemap index مُقسّم (كان 9.3MB/19,498 URL) بـ ISR 3600ث · `llms.txt`/`llms-full.txt` ISR ساعة · robots يحجب `/api/` وكل الأسطح الخاصة · crons اثنان بالضبط (سقف Hobby) وكلاهما ≤ مرة/يوم (قيد Hobby) · `exercise-mini` بكاش متصفح 24ث + جلب واحد لكل جلسة (inflight dedupe).

## 4. توصيات لوحة التحكم — بقرار المالك (§7)

### ت-1 (الأعلى أثرًا): قاعدة كاش Cloudflare لصور OG
في لوحة Cloudflare (نفس المنطقة، بجانب قاعدة «alkemos cache rules» الحالية) أضف قاعدة Cache Rule جديدة:
- **التعبير:** `(http.request.uri.path contains "/api/og-image/")` — أو أدق: `starts_with(http.request.uri.path, "/api/og-image/")`
- **السلوك:** Eligible for cache · **Edge TTL: 1 day** (البطاقات لا تتغير إلا بتغيير المقال) · Browser TTL: Respect existing (المسار يرسل أصلًا `max-age=86400, SWR 604800`)
- **الأثر:** زواحف السوشيال تعيد الجلب باستمرار (وكل قفزة WhatsApp/Telegram بطاقة = جلب كامل عند الأصل اليوم) → بعد القاعدة: ~99% من هذه الجلبات تُخدم من حافة CF بصفر دوال وصفر نقل من Vercel.
- **قانونيًا:** هذا تغيير لوحة CF → يُنفذ مع تحديث TECH_REFERENCE §5 و SECURITY §10 في نفس الفريم (§5.4/§3.8). **لا يلمس الكود إطلاقًا** — عناوين og:image الحالية تبقى كما هي.

### ت-2: قرار Speed Insights / Web Analytics ~~**ت-2 (بقرار المالك)**~~ — **مُنفّذ جزئيًا في VERCEL-USAGE-2 (انظر §7)**
الـ beacons لا تستهلك حصة **الموقع** (تذهب من متصفح الزائر إلى نطاقات Vercel مباشرة) لكنها تستهلك حصة **المنتجين المقيسين**. السؤال المتبقي على المالك: هل تستخدم لوحة **Web Analytics** في Vercel فعليًا؟ (`<Analytics />` ما زالت مُركّبة — 3.4K/50K فقط من حصتها). إن كان اعتمادك على Google Analytics — أمر إزالتها بريدي وسأنفذها في فريم لاحق.

### ت-3: رفع Edge TTL لـ HTML (مقايضة SEO)
الوضع الحالي 3600ث يوازن بين حداثة المحتوى وحمل الأصل. رفعه إلى 4–6 ساعات يقسم استدعاءات الرندر عند الأصل إلى النصف أو الثلث تقريبًا. مقايضة: المقالات الجديدة/التعديلات تظهر أبطأ للزائر العائد عبر PoP بارد. **قرار تسويقي بحت للمالك** — يُنفذ من لوحة CF فقط (تحديث قيمة القاعدة الحالية) مع تحديث التوثيق في نفس الفريم.

### ت-4 (لاحقًا): `getClaims()` بدل `getUser()` في middleware
النمط الحالي يتحقق من الجلسة عبر قفزة شبكة إلى Supabase لكل طلب يحمل كوكيز توثيق. الصيغة الحديثة من @supabase/ssr تدعم `getClaims()` بتحقق JWKS محلي (بلا قفزة). يوفر ~100–300ms من مدة كل دالة لكل طلب موثق. **تغيير حساس أمنيًا → جلسة مركّزة باختبارات مسارات كاملة (OAuth + بريد + admin) بأمر مالك صريح.**

## 5. حدود خطة Hobby — ما الذي يُستنزف فعلًا (للمراجعة في لوحة الاستخدام)

| البند | سقف Hobby | ماذا يحدث عند التجاوز | أكبر مساهم عندنا |
|---|---|---|---|
| Fast Data Transfer (النقل) | 100 GB/شهر | **إيقاف كل المشاريع** حتى دورة الفوترة التالية | HTML عند الـ miss + PNG الصور OG غير المكاش + صوت csp-report |
| Fast Origin Transfer | 100 GB/شهر | إيقاف | صغيرة (الأصول مكاشة CF) |
| Function Duration | 100 GB-hrs/شهر | تجاوز→دوال 403 | رندر SSR + EVO/AI (محمي) |
| Edge Requests | 1M/شهر | تجاوز→429 | كل طلب يمر (middleware يعدّ) |
| Image Optimization | 1,000 صورة مصدر | فشل `/_next/image` | **محصّن** (`unoptimized: true`) |
| Cron Jobs | 2، ومرة/يوم كحد أقصى | تجاوز→لا تعمل | عند السقف بالضبط (2) — لا تزد بدون ترقية |
| Web Analytics / Speed Insights | 2,500 حدث | تتوقف أو تفوتر | ت-2 |

**للفحص الذاتي:** Vercel Dashboard → Project alkemos → **Usage** tab (تفصيل Bandwidth/Function/Edge لكل مسار) — أو `Settings → Usage & Billing` على مستوى الفريق. الأرقام هناك هي الحكم؛ هذا التدقيق خريطة «لماذا».

## 6. القانون التبعي
- كل تعديل كود في هذا الفريم موثق بتعليقات `VERCEL-USAGE cleanup (2026-09-16)` في مواضعه.
- تنفيذ أي توصية من ت-1..ت-4 = فريم مستقل موثق (ت-1 وت-3 تحدّثان TECH_REFERENCE §5 + SECURITY §10 معًا — قانون §5.4).
- هذا الملف جزء من سجل التدقيق: `docs/VERCEL-USAGE-AUDIT-2026-09-16.md`.

---

## 7. VERCEL-USAGE-2 (2026-09-16 مساءً) — تقرير الاستخدام من لوحة المالك وردّ الطوارئ

> **السياق:** أرسل المالك لقطة لوحة Usage بترويسة تنبيهات **«Exceeded free resources»** مع «المشكلة كبيرة» — هذا القسم يوثق الاستجابة (فريم منفصل عن نطاق VERCEL-USAGE-1 أعلاه، بنفس اليوم).

### 7.1 الأرقام المُبلّغة (لوحة المالك)

| البند | الاستخدام/السقف | الحالة |
|---|---|---|
| **Deployment Storage** | **17.13 / 10 GB** | ❌ **متجاوز (171%)** |
| **Functions Storage** | **14.07 / 10 GB** | ❌ **متجاوز (141%)** |
| Speed Insights Events | 9.7K / 10K | ⚠️ 97% — سيتوقف هذا الشهر |
| Fluid Active CPU | 3h 36m / 4h | ⚠️ 90% |
| Image Optimization — Transformations | 4K / 5K | ⚠️ 80% |
| Fast Origin Transfer | 4.01 / 10 GB | 40% (مراقبة) |
| Function Invocations | 358K / 1M | 36% ✓ |
| Edge Requests | 263K / 1M | 26% ✓ |
| Image Optimization — Cache Writes | 21K / 100K | 21% ✓ |
| Web Analytics Events | 3.4K / 50K | 7% ✓ |

### 7.2 التشخيص الجذري للتجاوزين (Storage)

- سجل آخر تشغيل تنظيف (05:25 UTC): **80 عملية نشر جاهزة، حُذف 31، بقي 49** — سياسة المرحلة 145 (نافذة 48 ساعة + آخر preview) نجحت كما صُممت لكنها **رياضيًا لا تتسع** لمعدل النشر الفعلي (~28 نشرًا/يوم: 49 × ~350MB ≈ 17GB).
- عمليات النشر القديمة وحدها مصدر التجاوزين معًا: كل نشر يحمل حزم دواله (Functions Storage) + أصوله الثابتة (Deployment Storage).
- الـ workflow كان يعمل يوميًا بنجاح — المشكلة في **النافذة** لا في التنفيذ.

### 7.3 ما نُفّذ في هذا الفريم (كود + سياسة)

| # | التغيير | الملفات | الأثر المتوقع |
|---|---|---|---|
| ع-1 | **تضييق سياسة الاحتفاظ: نافذة 48→6 ساعات · previews 2→1** | `scripts/vercel-cleanup/vercel-cleanup.mjs` | ~9 عمليات نشر محتجزة بدل 49 ≈ **~3.2GB بدل 17GB** — تحت السقف مع هامش ~3× |
| ع-2 | **تشغيل التنظيف مع كل push إلى main + مرتين يوميًا (01:00+13:00 UTC) + مدخلات dispatch (keep_hours/keep_previews/dry_run)** | `.github/workflows/vercel-cleanup.yml` | الاحتجاز يبقى ملتصقًا بأرضية النافذة بدل التسرب بين التشغيلات؛ الجدولة الثانية تغطي نشرات لوحة التحكم المباشرة |
| ع-3 | **إزالة `<SpeedInsights />` + الاعتمادية + مدخلات CSP (`vitals.vercel-insights.com`)** | `src/app/layout.tsx` · `package.json`/`bun.lock` · `vercel.json` | حصة Speed Insights تتوقف عن الاحتراق (9.7K/10K) — مصدر القياس المعتمد GA؛ الإرجاع = سطر واحد موثق |
| ع-4 | **`s-maxage=3600` على استجابة og-image** + **استثناء `/api/og-image/*` من middleware** | `src/app/api/og-image/[slug]/route.tsx` · `src/middleware.ts` | الـ Set-Cookie الذي كان الـ middleware يختمه على كل طلب زاحف بلا كوكيز كان **يعطّل أي كاش CDN** — الآن حافة Vercel تكاش PNG لساعة: صفر دوال/صفر Satori/صفر استعلام Supabase لكل إعادة جلب ضمن الساعة (يخفض Fluid CPU + Transformations + Invocations + Origin Transfer). يُسقط أيضًا قفزة `getUser()` المهدرّة لكل زاحف (بند #5 من خريطة الاستهلاك) |
| ع-5 | **إزالة `sharp` من الاعتماديات المباشرة** (صفر مستوردين — `next@16` يجلبها ذاتيًا عبر optionalDependencies عند الحاجة) + حذف صف `@vercel/og` المتقادم من جدول الاعتماديات (غير مثبتة منذ المرحلة 151) | `package.json`/`bun.lock` · `DEVELOPER_GUIDE.md` | نظافة توثيق/تثبيت — **بلا ادعاء توفير تخزين** (sharp لم تكن تُتتبع في حزم الدوال أصلًا) |

### 7.4 ما تبقّى على المالك (لوحات التحكم — §7/§5.4)

1. **ت-1 (أصبح أكثر إلحاحًا):** قاعدة كاش Cloudflare لـ`/api/og-image/*` (TTL حافة يوم واحد) — الكود لم يعد عائقًا (الاستجابة الآن cookie-free مع s-maxage)؛ القاعدة ترفع التغطية من ساعة (حافة Vercel) إلى يوم + تصفير نقل الأصل من Vercel ذاته.
2. **إعادة نشر يدوية إن لزم:** لو ظهر نشر الكوميت الجديد «blocked/failed» في لوحة Vercel بسبب تجاوز التخزين لحظة البناء — انتظر حتى تكتد عمليات التنظيف (بضع دقائق) ثم اضغط **Redeploy** على آخر نشر، أو انتظر أول push تلقائي (خط المدونة) — سيقوم الـ workflow الجديد بالتنظيف قبل اكتمال أي بناء لاحق.
3. **مراقبة عداد Usage بعد التنقية الأولى:** يُتوقع الهبوط من 17GB إلى ~3-4GB خلال ساعة من أول تشغيل بالسياسة الجديدة (عدادات Vercel قد تتأخر دقائق).
4. **ت-3 (اختياري للـ CPU):** رفع Edge TTL للـ HTML من 3600ث إلى 4-6 ساعات يقسم رندرات الأصل على الاستهلاك المتبقي من Fluid Active CPU (3h36m/4h) — مقايضة حداثة/حمل تسويقية كما وثقتها ت-3 أصلاً.

### 7.5 قانون الفريم
- ع-3/ع-5 إزالات موثقة بمسار إرجاع من سطر واحد في تعليقات مواضعها (layout.tsx / next.config.ts).
- ع-4 لا يغيّر أي URL مفهرس (الاختبارات تثبّت `/api/og-image/...` — لم تُلمس) ولا سلوك مستخدم حقيقي (البشر يحملون كوكيز أصلاً فيرَون الـ middleware كالسابق على كل الصفحات الأخرى).
- الأثر المالي: صفر — كل التغييرات داخل خطة Hobby.
