# CI_GATES.md — البوابات الآلية: كل دفعة بتتدقق

> **المنشأ (Provenance):** أُنشئ بأمر المالك المباشر 2026-09-03 (Phase 111 — «استخرج شرح البوابات الآلية (CI Gates) من ملف README.md أو AGENTS.md، وانقل هذا الشرح كاملاً إلى ملف CI_GATES.md»).
> **مصادر النقل:** قسم «🛡️ Automated Quality Gates» من `README.md` (النص الأساس) + القوانين ذات الصلة من `AGENTS.md` (§3.5 أوامر التحقق القانونية · قانون مكافحة الانحدار · قانون لون التشغيل الصادق · مناعة الجدولة) + حادثة Phase 110 الملاحظة (npm arborist).
> **تحصين (2026-09-19 — المرحلة 234، خطة الترحيل Phase 2):** انسحب الثابت اليدوي `WORKLOG_TAIL_BASELINE` (ثلاث رفعات يدوية: 216/226/232 — فشل مرة كل ~10 مراحل: F-01/F-14) لصالح ثابت مشتق من النافذة ذاتها؛ فحص K جديد لمخطط مدخلات worklog؛ تقرير بايتات STATE.md في سطر التقرير. المصدر: `docs/DOCS-CONTEXT-MIGRATION-PLAN-2026-09-19.md` §5.
> **تحصين II (2026-09-19 — المرحلة 237، خطة الترحيل Phase 5 — الدوران (ب)):** سقف بايتات STATE.md أصبح **فشلًا صلبًا عند 32,000 بايت** (كان تحذيرًا عند 48KB)؛ فحص K (هيكل §12.5.1) أصبح **فشلًا فوريًا** بعد تطبيع المنطقة الحية ودوران الذيل التاريخي إلى الأرشيف (143 مدخلاً حرفيًا). المصدر: الخطة §8.
> **تحصين III (2026-09-24 — المرحلة 274، DOC-REMEDIATION):** استكمل جدول سجل الأعمال بأربعة workflows كانت غائبة (external-mirror · storage-backup · storage-inventory · storage-restore-drill) وصُحح تواتر تنقية Vercel (كل ساعة لا مرتين يوميًا — إصلاح م-08)؛ وبوابة المعرفة docs_audit.py توسعت بتسعة فحوص جديدة مضادة للانجراف (تفصيلها بمدخل worklog المرحلة 274).
> **العلاقة بالقانون:** `README.md` يبقى الواجهة العامة للبوابات و`AGENTS.md` يبقى ملف القوانين — هذا الملف مرجع تجميعي تعليمي بنفس مبدأ TECH_REFERENCE.md، وأي تعارض يفصل فيه هرم §12.8 (الكود أولًا).

---

## 1. الفكرة (النص المنقول من README)

الريبو مش معتمد على النوايا الحسنة — معتمد على **بوابات**.
مع كل push وكل pull request، GitHub Actions بتعيد اشتقاق الحقيقة من الكود نفسه، وبتفشل بصوت عالي على أي كذبة: مرآة schema انزاحت، وثيقة عدت، معرّف متقاعد رجع يظهر، أو ميجريشن هتكسر الداتابيز. البوابة الواقفة بتمنع الدمج — على runners بتاعة جيت هاب نفسها، مستقلة تمامًا عن أي جهاز محلي أو جلسة AI.

**كل بوابة تحت اتولدت من حادثة إنتاج حقيقية، وبعدين اتحولت لكود دائم:**

| البوابة | بتشتق / بتلقط إيه | الحادثة اللي بنتها |
|---|---|---|
| **Schema-drift audit** — `scripts/migration_audit.py` | بيفكك كل ميجريشن، ويقارن الـschema الناتج بمرآة `types.ts` الميدانية + baseline مقبول موثق؛ أي جدول/عمود فانتوم أو ناقص **جديد** = فشل الدفع (وضع `--ci`) | أعمدة فانتوم في المرآة وقّفت خط ميجريشنز الإنتاج الحي (`42703`) وكسرت مسارات أعضاء بصمت |
| **Registry parity** — `scripts/docs_parity.py` | بيعيد عدّ الميجريشنز على القرص كل تشغيل — ترويسة سجل `INDEX.md` لازم تتساوى مع حقيقة الفايل سيستم | الوثائق اتجمدت على مرحلة قديمة والمنتج فضل يشتغل؛ الانجراف عدّ شهور من غير حد يشوفه |
| **Knowledge operating system** — `scripts/docs_audit.py` | سلامة `STATE.md` (≤100 سطر، أقسامها الإلزامية، والكوميت المسجل فيه لازم يكون سلفًا لـHEAD — الحالة عمرها ما بتشاور لقدام) · تساوي المرحلة عبر STATE/PROGRESS/QA · **صفر** عدادات متغيرة في README/DEVELOPER_GUIDE (قانون المصدر الواحد للأرقام — الأرقام بيتها الكود) · ممنوع تكرار أرقام الأقسام في `AGENTS.md` · وثائق حية نحيفة بمؤشرات أرشيف · **(المرحلة 215 — فحوص الحقيقة H/I/J):** ترتيب worklog جديد-فوق (نافذة الـ12 العلوية بتاريخ/طور تنازلي + أحدث مدخل في القمة + ذيل مجمّد بثابت مشتق من النافذة ذاتها — المرحلة 234: لا مدخل أسفل النافذة أحدث من أقدم مدخل مؤرخ داخلها؛ الثابت اليدوي أُحيف) · ترويسات «Last updated/آخر تحديث» للوثائق الخمس المحكومة مقابل git (فوريًا فقط من 2026-09-16 — الانحراف الأقدم ملك المرحلة 217) · تجميد الأرشيف الحرفي (PROGRESS/QA_CHECKLIST صفر كوميتات من 2026-09-16) · **(المرحلة 234 — فحص K لخطة الترحيل Phase 2):** ترويسة «## Task ID:» مشوهة = فشل فوري (صنف F-02) · **(المرحلة 237 — الخطة Phase 5):** هيكل §12.5.1 الكامل لكل مدخل حي = فشل فوري عند النقص (التطبيع تم بالدوران) + سقف بايتات STATE صلب 32,000 (بدل تحذير 48KB) · **(المرحلة 274 — DOC-REMEDIATION):** تسعة فحوص مضادة للانجراف — أنماط أرقام موسعة للدليل (اختبارات/جداول اعتماديات/قيم ألوان) · Task IDs مؤرخة إلزاميًا (سد فجوة الإلحق بأسفل worklog) · بريد الديمو موحد مع بذر auth.ts · تواريخ سجل docs/README مقابل git لكل صف + وجود كل مسار · تغطية CI_GATES لكل ملف workflow · متطلب Node لا يقل عن engines · أي ذكر «OpenRouter + Groq» يستلزم NVIDIA · مسارات backtick بالوثائق الحاكمة يجب أن توجد · أوامر run الموثقة يجب أن تكون scripts حقيقية | حقائق مكتوبة في أماكن متعددة عدت بشكل مختلف — وملف القانون نفسه مرة شال §3.6 مكرر — والتدقيق العميق 2026-09-16 ضبط ترتيب worklog مكسور وترويسات كاذبة وأرشيف معدّل بعد التجميد والبوابة عمياء عنها كلها |
| **Anti-regression guard** — `scripts/check-stale-refs.sh` + `scripts/check-ui-wiring.sh` | فشل سريع على أي معرّف متقاعد رجع (روايتس متشالة، كومبوننتس ميتة، أسماء خطوط أنابيب قديمة) وعلى توصيل UI مكسور: أهداف fetch ميتة، أنواع jobs من غير معالج، معالجات من غير jobs، إنهاءات runner غير صادقة | روايتس AI متقاعدة فضلت شغالة بالتوازي مع الطابور الجديد — صنف «edits vanish» اللي المالك شافه بعينه |
| **Supabase Preview** | كل دفعة بتتطبق على داتابيز preview حقيقية عبر أكمال Supabase GitHub — ميجريشن واحدة فاشلة بتوقف الخط قبل ما توصل للإنتاج | اتعملت إثبات لايف: عمود واحد وحش جمّد دفتر الميجريشنز وسدّد صمت كل الميجريشنز اللي وراه |

**فوق البوابات، كل دفعة بتشغّل البطارية القياسية:** TypeScript `strict` · ESLint · مجموعة Vitest كاملة — كل البوابات zero-dependency وبتشرح الفشل inline (`::error::`) وبتخلص في ثواني.

**ليه ده مهم:** الوثائق مش ممكن تكذب، والمرآة مش ممكن تنزاح، والكود المتقاعد مش ممكن يرجع من الموت، وحالة المشروع الحالية دايمًا موجودة في [`STATE.md`](../STATE.md) — مدخل مُدار بقراءة 30 ثانية. وعلشان البوابات ساكنة جوه الريبو نفسه (`.github/workflows/` + `scripts/`)، بتسافر تلقائيًا مع أي نسخة نظيفة أو rebrand في المستقبل. ده اللي يخلي الريبو آمن للتطوير بمساعدة الوكلاء: أي جلسة، بشرية أو AI، تقدر تثق في الباب الأمامي من غير حفريات أثرية.

---

## 2. سجل الأعمال (Workflows) — البوابات عايشة فين

البوابات كلها ملفات تعريف داخل `.github/workflows/` والمنطق داخل `scripts/`:

| Workflow | الملف | الدور |
|---|---|---|
| **Quality gate** | `quality-gate.yml` | **Phase 141 (A-13):** أوامر AGENTS.md §3.5 الرسمية نفسها (tsc · eslint · vitest) على كل push/PR — تثبيت `bun install --frozen-lockfile` (شجرة الإنتاج) والتشغيل على Node 22 |
| Docs & schema parity gate | `docs-parity-gate.yml` | يشغّل `docs_parity.py` ثم `docs_audit.py` مع كل push/PR |
| Anti-regression guard | `guard-stale-refs.yml` | يشغّل `check-stale-refs.sh` + `check-ui-wiring.sh` مع كل push/PR |
| Supabase Preview | (تكامل Supabase GitHub، مش ملفنا) | تطبيق الميجريشنز على preview مع كل push — الفشل يوقف الخط |
| Process AI jobs | `process-ai-jobs.yml` | عامل طابور `ai_jobs` (جدولة + dispatch) — بيفشل بصدق لو أي job فشل نهائيًا |
| Auto-publish AR / EN article | `blog-post-ar.yml` / `blog-post-en.yml` | خط أنابيب المدونة v3 منفصل اللغات (فتحة يومية واحدة لكل لغة — Phase 119) |
| Remediate blog images | `remediate-blog-images.yml` | إعادة توليد صور المقالات القديمة عبر خط السلامة (workflow_dispatch) |
| Daily DB backup | `db-backup.yml` | (P3-6 — المرحلة 217؛ الجدول صار كاملًا بعد ما كان ناقصًا ورش العمل دي) لقطة يومية لكل الجداول العامة + auth لمستودع نسخ خاص — بديل الطبقة المدفوعة الغائبة في خطة Supabase المجانية (مُوثّق README — قسم Data layer) · مجدولة 05:30 UTC |
| EVO weekly eval | `evo-weekly-eval.yml` | EVO-5: مجموعة مرجعية AR/EN تُجاب بالبرومبت الحقيقي ويقيّمها نموذج قاضٍ رخيص إلى `evo_eval_runs` — تشغيل بصفر أسئلة مُقيَّمة يخرج أحمر بصدق (قانون EVO-5) |
| EVO weekly learning | `evo-weekly-learning.yml` | EVO-4: إعادة تجميع معرفة المنصة الغذائية من الخطط الحقيقية (E1/E2) وتعلّم أنماط الاستبدال (E3) إلى `evo_nutrition_patterns` — أمر المالك «نعم يتعلم من كل شيء» |
| Legacy AR cleanup | `legacy-ar-cleanup.yml` | المرحلة 175 («ابدا التنظيف»): تنظيف جودة مقالات AR الليجاسي على دفعات (workflow_dispatch) — قوائم انتظار مقيدة ببوابات جودة المحتوى |
| Meta-title remediation | `meta-title-remediation.yml` | المرحلة 181: إعادة قطع أحادية `meta_title` المخزّنة عبر clampMetaTitle المحسّن — لقطة تشغيل بلايست (workflow_dispatch) |
| Retro-pair blog | `retro-pair-blog.yml` | الاقتراح 3 («نفذ المقترح ٣»): إقران مكتبة المدونة ما قبل-157 بأزواج hreflang رجعيًا بنفس مصافحة P5 (workflow_dispatch) |
| Storage backup | `storage-backup.yml` | (P0-3(c) من خطة تقوية الاستعادة — أمر المالك 2026-09-20 مساءً) نسخة يومية لملفات Supabase Storage الفعلية إلى مستودع خاص مع manifest ‏sha256 لكل ملف · مجدولة 06:00 UTC |
| Storage inventory | `storage-inventory.yml` | (P0-3(b)) جرد يومي لكل الـbuckets والكائنات إلى مستودع النسخ — كشف صامت للفقد/النمو · مجدولة 05:45 UTC |
| Storage restore drill | `storage-restore-drill.yml` | (Drill #2 — أمر المالك «التحقق من إمكانية استعادتها») إثبات استعادة غير إتلافي من البداية للنهاية لنسخة Storage (رفع لمقطع مؤقت + قراءة sha256 + تنظيف دائم) · عند الطلب (workflow_dispatch — لا جدولة، الحفر عمدًا على الطلب) |
| Vercel deployment purge | `vercel-cleanup.yml` | (Phase 145 + شدد VERCEL-USAGE-2 ثم VERCEL-USAGE-3) تنقية نشرات Vercel المتقاعدة مع كل push + كل ساعة (على الدقيقة 7) — حماية حصة النشر المجانية (كان «مرتين يوميًا» قبل VERCEL-USAGE-3) |
| External mirror | `external-mirror.yml` | (P0-1 من خطة تقوية الاستعادة 2026-09-20) مرآة أسبوعية للريبوين إلى مستودعات خارجية خاصة — **خامدة بالتصميم** حتى يكمل المالك الإعداد الموثق بdocs/RECOVERY-MIRROR-SETUP.md · مجدولة الأحد 06:00 UTC |

> قواعد ملزمة للمهام الجديدة على البوابات: أي سكربت ي referenced بـworkflow لازم يتدفع **في نفس الكوميت** (مناعة الجدولة/الحارس — «حارس مش متكمّم مش حارس»)، وأي تعديل يلمس التشغيل يُختبر بـdispatch يدوي قبل الاعتماد عليه.
>
> **تثبيت Bun المقفل (Phase 141 / A-12):** كل workflows التشغيلية الأربعة (blog EN/AR · ai-jobs · remediate) بيثبتوا الاعتماديات الآن بـ`bun install --frozen-lockfile` عبر `oven-sh/setup-bun@v2` مقيد بـ1.3.14 — نفس `bun.lock` اللي بيبني منه Vercel والمحلي. ده بيقفل طبقة انجراف «npm بتحل شجرة مختلفة عن الإنتاج» اللي وصفها §5 تحت. وقت التشغيل بيفضل Node 22 (خطوات tsx/npx زي ما هي).
>
> **درس التشغيل الأول (Phase 141.1):** أول تشغيل حي لبوابة الجودة (run 34078054217) وقف على TS2307 لكل استورادات الصور — السبب: `next-env.d.ts` **متجاهَل في .gitignore** (بيتولّد فقط من `next build/dev`، وهو بيت تصريحات أنواع الصور من `next/image-types/global` — درس Phase 119 نفسه). البوابة الآن بيكتب الـstub القياسي قبل tsc — خطوة حتمية مستقلة عن أي build.

## 3. أوامر التحقق القانونية (من AGENTS.md §3.5 — المرجع الحاكم)

```bash
npx tsc --noEmit          # 1. TypeScript — 0 أخطاء
npx eslint .              # 2. ESLint — 0 أخطاء
npx next build            # 3. بناء Next.js — exit 0 (لو الشغل لمس rendering/routes)
git push origin main      # 4. دفع للأمام فقط (ممنوع --force بدون توجيه صريح من المالك)
git fetch origin --quiet && git rev-parse HEAD && git rev-parse origin/main
                          # 5. تحقق المزامنة — الاثنين لازم يتطابقوا
git status --short        # 6. شجرة العمل نظيفة
```

«يتجمّع» مش نفسها «يشتغل» — التحقق الوظيفي > smoke test > فحص الأنواع. أي route اتلمس بيتسموكي محليًا بـcurl قبل أي ادعاء نجاح.

## 4. قوانين مكملة من AGENTS.md تخص صحة البوابات

- **قانون مكافحة الانحدار (§8):** إيقاف أي شيء (route/component/script/step) = `git rm` + كنس المستدعين + تصحيح التعليقات في **نفس الكوميت**، صفر ملفات شواهد قبور، ثم `check-stale-refs.sh` يطلع 0 قبل الدفع؛ والحارس يعيد الفحص على كل دفعة. عند «سلوك رجع للوراء» افتح `/api/build-info` وقارن `commitShort` بأحدث SHA على main — عدم تطابق = تأخر/فشل deploy، ممنوع تصحيح كود ضد deploy قديم.
- **لون التشغيل الصادق (§8):** أي runner داخل `scripts/*-runner/*` لازم يحمل مسار exit غير صفرى صريح (مفروض بـCI) — تشغيل أخضر معنى الحقيقي الوحيد: `done=N failedPermanent=0`.
- **مناعة الجدولة (SCHEDULE HEALTH LAW):** جدول جيت هاب ممكن يسجّل خروج صامت لكل workflows المجدولة — أي بلاغ «المدونة واقفة» بيبدأ بجنائيات الجدولة (`GET /actions/runs?event=schedule`) مش بإعادة قراءة كود الأنابيب؛ والعلاج: تمكين كل workflow + كوميت يلمس ملفاتها لإعادة التسجيل؛ والظهر المستقل: `/api/cron/dispatch-pipelines` (فشل مغلق، CRON_SECRET) بيكمّل حصة اليوم الناقصة (مقال واحد لكل لغة — Phase 119) يوميًا 23:40 UTC (بعد فتحتَي اليوم لكل لغة وبعد فترة السماح 90 دقيقة — المرحلة 171: الفتحة تُعد متوقعة بعد ساعتها بـ90 دقيقة، والتغطية = أقصى (تشغيلات غير فاشلة، مقالات منشورة فعليًا اليوم)، والقانون نفسه يُفرض في P5: صف آلي لا ينشر مقالًا ثانيًا بنفس اليوم — صفوف الكوتش معفاة).
- **حادثة Phase 110 الموثقة (2026-09-03):** worker الـAI قعد أحمر على «Install dependencies» بانهيار npm 10 arborist (`Cannot read properties of null (reading 'edgesOut')`) ضد metadata الريجستري بتاريخ اليوم — الريبو بلا lockfile فالتثبيت بيحل فريش كل مرة، وnpm 11.17 حل نفس الشجرة سليمة محليًا → خطوة «Bump npm (npm install -g npm@11)» اتضافت قبل التثبيت في كل الـworkflows المكشوفة الأربعة. الدرس: فشل «Install dependencies» في CI يُشخّص كطبقة بنية تحتية أولًا قبل الشك في كوميت الكود. **الحل الجذري (Phase 141 / 2026-09-07):** طبقة الحل المؤقت ده (Bump npm + حل فريش بلا lockfile) اتبدلت بـ`bun install --frozen-lockfile` في الـworkflows الأربعة — نفس قفل الإنتاج (`bun.lock`)، فانجراف الشجرة مستحيل أصلاً وحادثة من الفئة دي مقفولة.

## 5. فلسفة البوابات في سطر

> حارس مش متكمّم مش حارس · نتيجة أخضر مش صادقة = أحمر مقنّع · الوثائق مش ممكن تكذب · والبوابات بتسافر مع النسخة النظيفة تلقائيًا.
