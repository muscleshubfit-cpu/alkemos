# EMERGENCY-RECOVERY-HARDENING-PLAN-2026-09-20 — خطة تقوية الاستعادة

> **النوع:** خطة تنفيذ (للتوثيق أولًا ثم التنفيذ بموجات بأمر المالك) مبنية **حصريًا** على العوائق المُثبتة في `docs/EMERGENCY-RECOVERY-AUDIT-2026-09-20.md` — التقرير هو مصدر الحقيقة؛ لا بند هنا بلا سابقة مُثبتة فيه، ولا بند نظري إضافي.
> **تاريخ الاعتماد:** 2026-09-20 · **الآمر:** المالك (نفس جلسة تثبيت التقرير).
> **قانون التنفيذ:** كل تنفيذ داخل GitHub فقط (ملفات `docs/` + `scripts/` + `.github/workflows/` + ملفات البروتوكول `worklog.md`/`STATE.md`/سجل `docs/README.md` بالحد الأدنى الإلزامي). **لا** لمس إعدادات Vercel/Cloudflare/Supabase، **لا** تعديل application code تحت `src/`، **لا** أي سر أو توكن أو قيمة حساسة في أي ملف، **لا** إنشاء الـ Mirror فعليًا (يتطلب قرار/توكن خارجيًا — يُجهَّز فقط)، **لا** نسخ ملفات Storage الفعلية (قرار مالك)، **لا** استعادة على مشروع Supabase حقيقي (قرار مالك). **(تحديث 2026-09-20 مساءً — أمر المالك):** رُفع منع «نسخ ملفات Storage الفعلية» صراحة بأمر «أنشئ أو أكمل آلية النسخ الاحتياطي الآلية لجميع ملفات Supabase Storage الفعلية … مع التحقق من إمكانية استعادتها» — P0-3(ج) منفّذ (RUNBOOK §5.1)؛ الدريل غير المدمّر يعمل على باكت مؤقتة في نفس المشروع (لا مشروع جديد)؛ بقية القوانين كما هي.
> **قاعدة الإيقاف:** يتوقف أي بند عند خطر حقيقي أو حاجة صلاحية/قرار خارجي من المالك، ويُوثَّق سبب التوقف.

---

## 0. خريطة البنود ← العوائق المُثبتة

| بند الخطة | العائق/النتيجة المُثبتة في التقرير | قابل للتنفيذ آليًا (وكيل داخل GitHub)؟ |
|---|---|---|
| P0-1 External Git Mirror | B2 (حساب GitHub واحد يحمل كل شيء — نقطة فشل شاملة) | **تجهيز فقط** (سكربت + workflow + دليل)؛ التنفيذ بقرار مالك (مزود خارجي + توكنه) |
| P0-2 توثيق مصادر Secrets | B1 (قيم PayPal وGA وAdSense في Vercel فقط) + N1–N4 + B8 + جزء B9 (فجوة `.env.example`) | **نعم** كاملًا |
| P0-3 حماية وتوثيق Supabase Storage | B4/N5 (ملفات Buckets — بما فيها `coach-public` — بلا أي نسخة) | **نعم بالكامل بعد قرار المالك (2026-09-20 مساءً): (أ) التوثيق + (ب) الجرد اليومي + (ج) نسخ الملفات الفعلية اليومي 06:00 UTC + أدوات الاستعادة والدريل — B4 مغلق** |
| P0-4 توثيق إعدادات Supabase Auth | B6/N7 (إعدادات اللوحة: Google OAuth + SMTP + site_url غير موجودة في git) | **نعم** كاملًا |
| P1-5 مسار استعادة Supabase الكامل | تقرير §6.2 + B5/N6 (كلمات المرور غير قابلة للنسخ) + §2.3 | **نعم** (توثيق) — الاستعادة الفعلية مشروع جديد بقرار مالك |
| P1-6 Recovery Drill غير مدمر | توصية التقرير P1#5 (تدقيق قابلية الاستعادة دوريًا) | **نعم** (قراءة فقط) |
| P2-7 إصلاح انحرافات التوثيق | B9 (sin1→fra1 في DEVELOPER_GUIDE · فجوة `.env.example` · حقل iad1 العرضي) | **نعم** |
| P2-8 صلاحية BACKUP_REPO_TOKEN | B3 (انتهاء محتمل يوقف النسخ اليومي) | **الدليل الوظيفي منفّذ (2026-09-20: 18/18 نسخة + 4/4 جرد نجاحًا، أحدثها اليوم)** — فحص تاريخ الانتهاء يبقى بواجهة GitHub عند المالك (لا مسار API له — حد المنصة؛ خطوات الواجهة المرشدة في `docs/RECOVERY-SECRETS-SOURCES.md` §4) |
| P2-9 قرار خطة Hobby | B7 (تجاوزات 171%/141% موثقة 2026-09-16 — خطر إيقاف بنيوي) | **لا** — قرار مالكي |
| P2-10 سحب التوكنات الميتة | B8 (توكن GitHub الأول + توكن Vercel الأول غير صالحين) | **أعيد إثبات موتهما حيًا (2026-09-20: 401 و404)** — السحب نفسه من لوحات المالك فقط (لا مسار API — التوكنات الميتة لا تشكل خطرًا استخداميًا؛ خطتا السحب المرشدتان في `docs/RECOVERY-SECRETS-SOURCES.md` §4) |

---

## 1. بنود P0

### P0-1 — External Git Mirror (سابقة: B2)

| البند | التفصيل |
|---|---|
| **الهدف** | نسخة mirror كاملة (تاريخ + فروع) للمستودعين العام `alkemos` والخاص `musclehubeg-backups` على مزود git **خارج حساب GitHub الحالي**، تُحدَّث آليًا بوتيرة أسبوعية على الأقل — لكسر «الحساب الواحد يحمل كل شيء». |
| **مكان التنفيذ** | (أ) تجهيز آلي داخل المستودع: `scripts/external-mirror/mirror.sh` + `.github/workflows/external-mirror.yml` (ساكن حتى ضبط المدخلات) + `docs/RECOVERY-MIRROR-SETUP.md`. (ب) التنفيذ الفعلي: مزود خارجي يختاره المالك (حساب GitLab/Bitbucket/حساب GitHub آخر) — **مستودعات خاصة إلزاميًا** (النسخ تحتوي PII). |
| **الاعتماديات** | من المالك: (1) قرار المزود، (2) توكن رفع على المزود الخارجي يُضاف كـ Secret باسم `EXTERNAL_MIRROR_TOKEN`، (3) متغيرا Actions `MIRROR_REMOTE_MAIN`/`MIRROR_REMOTE_BACKUPS` (عناوين المستودعين الخارجيين). من المستودع: أسرار موجودة مسبقًا (`BACKUP_REPO_TOKEN` لاستنساخ الخاص). |
| **طريقة التحقق** | التجهيز: تشغيل dry محلي للسكربت + فحص YAML + فشل workflow الساكن برسالة الإعداد الصحيحة (سابقة `vercel-cleanup` القائمة). التنفيذ (بعد ضبط المالك): استنساخ من المزود الخارجي ومطابقة HEAD SHA مع origin/main + سجل تشغيل أخضر أسبوعيًا. |
| **معيار النجاح** | mirror خارجي خاص يحوي آخر commit خلال ≤7 أيام من أي دفعة، وتمرين استنساخ مطابق واحد موثق في تقرير drill لاحق. |
| **المخاطر** | إنشاء المستودع الخارجي عامًا (تسريب كود + PII) — يُمنع بالدليل؛ نسيان التجديد — يعالجه الجدولة الأسبوعية الآلية؛ توكن خارجي واسع الصلاحية — يُقيد بـ repo read/write على مستودعَي الـ mirror فقط. |
| **صلاحيات خارج GitHub؟** | **نعم للتنفيذ** (حساب/توكن مزود خارجي بيد المالك) — لهذا يُنفَّذ التجهيز فقط في موجة W4 ويُسلَّم المتبقي بقرار مالك. |

### P0-2 — توثيق مصادر Secrets لإعادة الاستعادة (سابقة: B1 + N1–N4 + B8 + جزء B9)

| البند | التفصيل |
|---|---|
| **الهدف** | مرجع دائم داخل المستودع (`docs/RECOVERY-SECRETS-SOURCES.md`) يحدد لكل متغير/سر مطلوب لتشغيل المشروع: مصدر قيمته الأصلية (لوحة أي مزود)، بيئة استخدامه، من يستطيع جلبه، وموضعه في سيناريو فقدان Vercel — **بلا أي قيمة مخزنة**. يشمل جدول صلاحية التوكنات المُثبت في التقرير (B8) وقاعدة «أسرار GitHub Actions للقراءة الآلية فقط». |
| **مكان التنفيذ** | ملف docs جديد + إكمال `.env.example` بالأسماء الناقصة المُثبتة (EVO_CRON_SECRET · EVO_FOLLOWUP_FROM · EMAIL_REPLY_TO · EVO_PARTNER_API_ENABLED · SUPABASE_ANON_KEY) مع تعليقات المصدر، وتعليم الميت (SUPABASE_SECRET_KEY/SUPABASE_PUBLISHABLE_KEY/SUPABASE_JWT_SECRET) والمتروك عمدًا (UNSPLASH/PIXABAY/COACH_EMAILS — اختياري بلا قيمة إنتاجية). |
| **الاعتماديات** | جرد الأسماء في التقرير §8.1 (مصدر الحقيقة) — لا فحص خارجي جديد. |
| **طريقة التحقق** | مطابقة قائمة الأسماء 1:1 مع §8.1 + مسح آلي أن الملفات الجديدة/المعدلة خالية من أنماط القيم (`sk-` · `ghp_` · `gho_` · `github_pat_` · `vcp_` · `sbp_` · `cfut_` · `eyJ` · سلاسل base64 طويلة) + مراجعة بشرية قبل الرفع. |
| **معيار النجاح** | كل اسم من مدخلات Vercel الحية له صف مصدر واحد واضح، و`.env.example` يغطي القائمة كاملة تعليقيًا، وصفر نمط قيم في الملفات المرفوعة. |
| **المخاطر** | كتابة قيمة سهوًا (يمنعها المسح الآلي القبل-رفع)؛ تقادم المرجع عند إضافة متغيرات مستقبلًا (يُربط بقانون تكافؤ التوثيق §3.8 — أي متغير جديد يُوثق مصدره في نفس فريم إضافته). |
| **صلاحيات خارج GitHub؟** | **لا** — توثيق داخل المستودع فقط؛ جلب القيم نفسها يبقى بيد المالك وقت الاستعادة (موثّق في المرجع نفسه). |

### P0-3 — حماية وتوثيق Supabase Storage بما فيه coach-public (سابقة: B4/N5)

| البند | التفصيل |
|---|---|
| **الهدف** | **(ج) منفّذ بأمر المالك 2026-09-20 مساءً — انظر RUNBOOK §5.1 + مدخل STORAGE-FILE-BACKUP-2026-09-20** · (أ) توثيق جرد Buckets الأربعة المُثبتة (`questionnaire-photos` · `progress-photos` · `receipts` — خاصة · `coach-public` — عامة) بتعريفاتها في الـ migrations وسياستها ومسارات الكود المستخدمة لها، داخل runbook الاستعادة. (ب) **جرد ميتاداتا يومي آلي**: عدّاد ملفات + حجم إجمالي لكل bucket (بلا أسماء ملفات — صفر PII) يُخزَّن بجوار النسخ اليومية في المستودع الخاص `musclehubeg-backups/storage-inventory/YYYY-MM-DD.json` — يمنح رؤية دائمة لما هو المعرّض للفقدان ويقيس نمو الملفات. (ج) عرض خيارات نسخ الملفات الفعلية (سحب دوري مضغوط إلى المستودع الخاص / رفع Supabase Pro للنسخ الأسبوعية / تصدير دوري إلى مزود كائنات) — **قرار مالك** بالكلفة/الخصوصية/الحجم. |
| **مكان التنفيذ** | (أ) قسم Storage في `docs/SUPABASE-FULL-RECOVERY-RUNBOOK.md`. (ب) `scripts/storage-inventory.mjs` (سكربت جديد معزول) + `.github/workflows/storage-inventory.yml` (workflow جديد مجدول 05:45 UTC بعد نسخة 05:30 — **لا يمس** `db-backup.yml` أو `db-backup.mjs` إطلاقًا؛ خطوة الجرد best-effort بـ continue-on-error). (ج) قسم الخيارات في نفس الـ runbook. |
| **الاعتماديات** | أسرار Actions موجودة مسبقًا: `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (قراءة Storage API) + `BACKUP_REPO_TOKEN` (دفع ملف الجرد للمستودع الخاص). لا أسرار جديدة. |
| **طريقة التحقق** | السكربت: تشغيل محلي بمتغيرات وهمية يثبت صفر نداءات شبكية في وضع الفشل + صحة JSON الناتج. الـ workflow: تشغيل `workflow_dispatch` واحد موثق النتيجة يُظهر ملف الجرد في المستودع الخاص + إثبات أن فشل الجرد لا يعطل شيئًا (continue-on-error). الجرد اليومي: ظهور ملف بتاريخ الغد في مساره. |
| **معيار النجاح** | ملف جرد يومي لكل bucket (عدّاد + حجم + حالة ok/error) بجوار النسخ اليومية، وصفر تأثير على نجاح النسخة الأساسية (استمرار السلسلة اليومية الخضراء)، ووثيقة جرد مكتملة للأربعة. |
| **المخاطر** | المساس بخط النسخ الحرج — يُمنع بالعزل التام (سكربت منفصل + workflow منفصل + عدم لمس ملفات db-backup إطلاقًا)؛ PII في المخرجات — يُمنع بجمع العدادات والأحجام فقط؛ تكاسل الجرد عن الفشل بصمت — سجل الجرد يحمل ok/error لكل bucket والتقرير اليومي يظهر الفارق. |
| **صلاحيات خارج GitHub؟** | **لا** لـ(أ)+(ب). **نعم (قرار مالك)** لـ(ج) نسخ الملفات الفعلية. |

### P0-4 — توثيق إعدادات Supabase Auth اللازمة للاستعادة (سابقة: B6/N7)

| البند | التفصيل |
|---|---|
| **الهدف** | توثيق كل إعداد Auth مُدار من لوحة Supabase (غير موجود في git) والمقيس حيًّا في التقرير §6.3: site_url · مزود البريد + SMTP المخصص (Brevo) · Google OAuth · سياسة التأكيد (autoconfirm=false + تريغر 0074 الكودي) · فتح التسجيل — بقيم وصفية ومصدر كل قيمة عند الاستعادة، داخل قسم مخصص في الـ runbook. |
| **مكان التنفيذ** | قسم «إعدادات Auth» في `docs/SUPABASE-FULL-RECOVERY-RUNBOOK.md`. |
| **الاعتماديات** | قياسات التقرير §6.3 + الميجريشن 0074 (المصدر الكودي الموثق) — لا فحص جديد. |
| **طريقة التحقق** | مراجعة أن كل حقل مقيس له صف: القيمة الوصفية + مصدر الاستعادة (Google Cloud Console / حساب Brevo / إدخال يدوي) + أثر غيابه. |
| **معيار النجاح** | قسم يُمكّن من إعادة بناء إعدادات Auth على مشروع Supabase فارغ دون معرفة خارجية سوى لوحات المزودين، ويصرّح بالقيود الموروثة (كلمات المرور — B5). |
| **المخاطر** | تقادم التوثيق عند تغيير الإعدادات من اللوحة دون فريم — يُخفف بإحالة القارئ للتحقق الحي وقت الاستعادة (الفحص قراءة فقط وموثق في التقرير §6.3). |
| **صلاحيات خارج GitHub؟** | **لا** — توثيق فقط. (إعادة الإدخال الفعلية على مشروع جديد = ضمن سيناريو P1-5 الذي يتطلب قرار مالك.) |

---

## 2. بنود P1

### P1-5 — مسار استعادة Supabase عند فقد المشروع بالكامل (سابقة: تقرير §6.2 + B5/N6)

| البند | التفصيل |
|---|---|
| **الهدف** | Runbook خطوة-بخطوة (`docs/SUPABASE-FULL-RECOVERY-RUNBOOK.md`) لإعادة بناء قاعدة البيانات على مشروع Supabase جديد: (1) تفعيل تكامل GitHub → الميجريشنز التلقائية (`0000_*` + `YYYYMMDDHHMMSS_*`) · (2) القائمة الكاملة للميجريشنز اليدوية `RUN_ON_SUPABASE_*`/`RUN_ON_SUPABASE_ORIGINAL_*` بترتيب ملفاتها كما يفرضه `INDEX.md` · (3) `db-restore.mjs` من آخر لقطة (dry ثم --apply على هدف صحيح) · (4) إعادة إعدادات Auth (P0-4) · (5) إعادة تعريف الـ Buckets (0027 + 0037 idempotent) وحدود ملفات التخزين غير القابلة للاستعادة · (6) تسلسل فحص بعد الاستعادة + القيود المعلومة (كلمات المرور/الجلسات/الملفات). |
| **مكان التنفيذ** | docs/ (ملف واحد) + صف سجل في `docs/README.md`. |
| **الاعتماديات** | `supabase/migrations/INDEX.md` (المرجع الحاكم) + ملفات الميجريشنز نفسها + `scripts/db-restore.mjs` + مستودع النسخ الخاص — كلها مُثبتة في التقرير. |
| **طريقة التحقق** | توليد جدول التصنيف برمجيًا من نظام الملفات (لا يدويًا) ومطابقته مع قانون التسمية في INDEX.md: عدد الملفات الكلي + التلقائي/اليدوي = الكل، بلا ملف بلا تصنيف؛ وتشغيل `migration_audit.py --ci` (التماثل مع types.ts) ضمن drill P1-6. |
| **معيار النجاح** | runbook يغطي كل ملف ميجريشن بتصنيفه وترتيبه، وكل خطوة لها أمر/مسار تحقق، وكل قيد غير قابل للاستعادة مصرّح به صراحة. |
| **المخاطر** | انحراف مستقبلي بين INDEX.md ونظام الملفات — تحرسه بوابات `docs_parity.py`/`migration_audit.py` القائمة؛ تنفيذ فعلي بلا أمر مالك — ممنوع بنص الخطة (التنفيذ الفعلي مشروع جديد = قرار موارد/كلفة). |
| **صلاحيات خارج GitHub؟** | **لا** للتوثيق. **نعم** للاستعادة الفعلية (إنشاء مشروع Supabase جديد + لوحات المزودين). |

### P1-6 — Recovery Drill غير مدمر (سابقة: توصية التقرير P1#5 — «تجربة استعادة دورية»)

| البند | التفصيل |
|---|---|
| **الهدف** | إثبات عملي موثق أول (Drill #1) لقابلية الاستعادة **بصفر كتابة على أي خدمة خارجية**: (1) استنساخ مستودع النسخ الخاص (قراءة فقط)؛ (2) تشغيل `db-restore.mjs` **dry-run** على آخر لقطة — يفك 53 ملف JSON ويحصي الصفوف بلا أي نداء شبكي (متغيرات بيئة وهمية غير فارغة — المسار الجاف لا يستخدمها)؛ (3) تشغيل `migration_audit.py --ci`؛ (4) بوابة الجودة المحلية من استنساخ نظيف (install + tsc + eslint + vitest + build إن سمحت البيئة) — إثبات أن GitHub وحده يبني المشروع؛ (5) توثيق النتائج في `docs/RECOVERY-DRILL-REPORT-2026-09-20.md` كأول عينة دورية قابلة للتكرار. |
| **مكان التنفيذ** | بيئة فحص محلية معزولة (خارج أي خدمة إنتاج) + ملف التقرير في docs/. |
| **الاعتماديات** | توكن قراءة للمستودع الخاص (موجود بيد المنفّذ) + سكربتات المستودع + آخر لقطة (موجودة). لا كتابة على Supabase/Vercel/Cloudflare بأي شكل. |
| **طريقة التحقق** | سجل تنفيذ حرفي بالأوامر والمخرجات المهمة داخل تقرير الـ drill + قابلية إعادة التشغيل بأوامر التقرير نفسها. |
| **معيار النجاح** | dry-run يستوعب كل ملفات اللقطة ويطابق عدّادات manifest · migration audit أخضر · بوابة الجودة المحلية خضراء (أو عائق بيئة موثق صراحة) · تقرير مرفوع للمستودع العام (لا يحوي أي سر). |
| **المخاطر** | خلط dry-run بـ `--apply` — يمنعه الافتراض الجاف للسكربت + تحذير بارز في التقرير؛ انكشاف PII من مخرجات اللقطة داخل التقرير — يُمنع بنقل العدادات فقط (أسماء الجداول وعدد الصفوف) بلا أي صف بيانات. |
| **صلاحيات خارج GitHub؟** | **لا** — قراءة فقط بالكامل. |

---

## 3. بنود P2 — عوائق إضافية مُثبتة (بلا أعمال نظرية)

### P2-7 — إصلاح انحرافات التوثيق (سابقة: B9) — **قابل للتنفيذ آليًا**
- **الهدف:** تصحيح ما أثبته التقرير حرفيًا: `DEVELOPER_GUIDE.md` §10 مثال المنطقة `sin1` → `fra1` (الحاكم `vercel.json`)؛ إكمال `.env.example` (ينفَّذ ضمن P0-2 بنفس الفريم)؛ تدوين حقيقة حقل `serverlessFunctionRegion: iad1` العرضي مقابل `fra1` التشغيلي في مرجع مصادر الأسرار.
- **التحقق:** تشغيل `docs_audit.py --ci` + `docs_parity.py --ci` محليًا قبل الرفع (نفس بوابات push).
- **معيار النجاح:** صفر ذكر sin1 في DEVELOPER_GUIDE · البوابات خضراء.
- **خارج GitHub؟** لا.

### P2-8 — صلاحية BACKUP_REPO_TOKEN (سابقة: B3) — **مالك فقط**
- **المطلوب:** فحص تاريخ انتهاء التوكن في GitHub → Settings → Developer settings → Fine-grained tokens (لا يُكشف عبر API — التحقق يدوي بحكم المنصة) + التجديد قبل الانتهاء + تعليق مراقبة أسبوعية أن آخر نسخة عمرها < 25 ساعة (لوحة Actions تُظهرها مباشرة).
- **لماذا خارجي:** قيمة الانتهاء غير قابلة للقراءة البرمجية، وتدوير الأسرار قرار مالك بطبيعته.

### P2-9 — قرار خطة Vercel Hobby (سابقة: B7) — **مالك فقط**
- **المطلوب:** قرار واحد من اثنين: ترقية الخطة (تُزيل خطر الإيقاف الهيكلي وتفتح نسخًا منصة لـ Supabase لاحقًا) أو تجهيز حساب/فريق Vercel احتياطي مُعد مسبقًا (مشروع مستورد بلا دومين) لتقليص RTO إلى دقائق. الاختيار مالي/تشغيلي بحت.

### P2-10 — سحب التوكنات الميتة (سابقة: B8) — **مالك فقط**
- **المطلوب:** سحب توكن GitHub الأول الميت وتوكن Vercel الأول الميت من لوحتي GitHub/Vercel (الإبطال قرار مالك)، والاكتفاء في التداول بالصالحين الموثقين في التقرير (ملحق أ). يمنع الالتباس لحظة الطوارئ.

---

## 4. موجّهات التنفيذ (Waves) — التزامات البروتوكول

| الموجة | المحتوى | مخرجات | Commit |
|---|---|---|---|
| **W0 (تثبيت)** | رفع التقرير كما هو + هذه الخطة | الملفان على origin/main | `docs:` واحد |
| **W1** | P0-2 + P0-4 + P1-5 + P2-7 (حزمة التوثيق المرجعي) | `RECOVERY-SECRETS-SOURCES.md` + `SUPABASE-FULL-RECOVERY-RUNBOOK.md` + `.env.example` + إصلاح DEVELOPER_GUIDE + صفوف السجل | `docs:` واحد |
| **W2** | P0-3 (ب) الأتمتة + تشغيل تحقق واحد | `scripts/storage-inventory.mjs` + `.github/workflows/storage-inventory.yml` + تشغيل dispatch موثق النتيجة | `ops:` واحد |
| **W3** | P1-6 تنفيذ Drill #1 | `RECOVERY-DRILL-REPORT-2026-09-20.md` + الأدلة | `docs:` واحد |
| **W4** | P0-1 التجهيز | `scripts/external-mirror/mirror.sh` + `.github/workflows/external-mirror.yml` + `RECOVERY-MIRROR-SETUP.md` (التنفيذ بقرار مالك) | `ops:` واحد |

**التزامات إلزامية لكل موجة (قوانين AGENTS.md):** مدخل worklog واحد بقالب §12.5.1 (≤60 سطرًا، مؤشر Task ID فريد، SHA + حالة الدفع) · صف/صفوف سجل docs/README.md لأي ملف توثيقي جديد بنفس الفريم · تحديث STATE.md فقط إن غيّرت الموجة حالة موصوفة فيه (الحد الأدنى) · رسالة كوميت ≤72 حرفًا عنوانًا و≤500 جسمًا وتؤشر لمدخل الـ worklog · تشغيل البوابات المحلية (`docs_audit` · `docs_parity` · `migration_audit` · `stale-refs`) قبل كل رفع · مسح أنماط الأسرار قبل كل رفع.

## 5. حدود هذه الخطة (ما لا تفعله أبدًا)

- لا تخزين أي سر/توكن/قيمة حساسة في أي ملف — أسماء ومصادر فقط (قانون P0-2 يمتد لكل موجاتها).
- لا تعديل application code تحت `src/` (السكربتات التشغيلية الجديدة معزولة في `scripts/`).
- لا لمس إعدادات Vercel/Cloudflare/Supabase أو أي خدمة خارجية (الاستثناء الوحيد: تشغيل workflow على GitHub Actions يقرأ Supabase قراءة فقط ويدفع ملف جرد للمستودع الخاص — ضمن P0-3).
- لا إنشاء الـ Mirror الفعلي ولا نسخ ملفات Storage الفعلية ولا استعادة على مشروع حقيقي — كلها بقرار مالك (P0-1/P0-3(ج)/P1-5).
- لا تنفذ أي بند خارج موجاته، وأي توقف يُوثَّق بسببِه في مدخل الـ worklog بنفس الفريم.
