# خطة معالجة تدقيق 2026-09-16 — Alkemos Deep Audit Master Plan

> **الحالة:** التنفيذ بدأ بأمر المالك 2026-09-16 («نفّذ Phase 215 بالكامل… أي بند 🔐 يحتاج قرارًا مني: توقّف قبله») — **المرحلة 215 (WAVE 1) مكتملة الآن بالكامل** — البنود غير الأمنية نُفِّذت أولًا، ثم أكملها أمر المالك الثاني 2026-09-16 («الغى التحذير ، اعتمد الخيار (ب) ، اوافق على تنفيذ p1-6/p1-7»): P1-5 أُغلقت بالخيار (ب) · P1-6/P1-7 نُفِّذا بموافقة §7 الصريحة. **WAVE 2 (216) و WAVE 3 (217) بانتظار أمر المالك.**
> **المصدر:** [`docs/DEEP-AUDIT-REPORT-2026-09-16.md`](DEEP-AUDIT-REPORT-2026-09-16.md) (التقرير الكامل بأدلته).
> **المرجع الحاكم:** AGENTS.md — كل بند أمني (موسوم 🔐) يتطلب موافقة المالك **قبل** التنفيذ وفق §7؛ كل مرحلة تنفذ IMPLEMENT → VALIDATE → DOCUMENT → COMMIT → PUSH (§12.2) مع بوابات §3.5.
> **التقسيم:** موجات تنفيذية؛ كل موجة = مرحلة واحدة مرقّمة (تبدأ من 215) قابلة للـ commit المستقل والتحقق الحي.

---

## WAVE 0 — فوري (اليوم، خارج المستودع — لا يحتاج مرحلة)

> **W0-1 — Dropped (2026-09-16):** بأمر المالك الصريح «الغى التحذير» — المالك اطّلع على تحذير نشر المفاتيح في المحادثة وقرر عدم تدويرها؛ سُجِّل الإسقاط هنا وفق عرف المشروع ولا يُعاد رفعه إلا بأمر جديد.

## WAVE 1 — المرحلة 215: أمان + كاش + حوكمة P1

> **حالة التنفيذ (2026-09-16 — أُغلقت المرحلة 215):** **P1-1 ✓ منفَّذ** · **P1-2 ✓ منفَّذ** (كلاهما abfd8357) · **P1-3 ✓ منفَّذ** (8aec3368) · **P1-4 ✓ منفَّذ** (7674a7c6) · **P1-8 ✓ منفَّذ** (d8f5e552) · **P1-5 ✓ مُغلق بالخيار (ب)** (أمر المالك «اعتمد الخيار (ب)» — توثيق Cloudflare كطبقة الكاش الرسمية: middleware.ts + next.config.ts + SECURITY.md §10 + TECH_REFERENCE §5) · **P1-6 ✓ منفَّذ** (موافقة §7 الصريحة — timingSafeSecretEqual من cron-auth.ts) · **P1-7 ✓ منفَّذ** (موافقة §7 الصريحة — سقوف zod: title ≤200 · result_data ≤10KB · plan_data ≤32KB معايرةً · broadcast title/body/link + userIds ≤1000).
>
> **نتيجة التحقق الخارجي لـ P1-5 (قراءة فقط — Cloudflare API + قياس حي):** Browser-Cache-TTL = **0 (Respect Existing Headers — ليس المصدر)** · لا Page Rules نشطة · قاعدة وحيدة «alkemos cache rules (SEO-GEO-4 2026-09-08)» (ruleset d9f2c38e…، آخر تحديث 2026-09-13 = المرحلة 189): `cache=true` + `edge_ttl override 3600` + `browser_ttl override 300` (أُضيفت بالمرحلة 189 وفق §12.46-د) والتعبير = قائمة استثناءات SEO-GEO-4 نفسها + بلا مسارات بنقاط. **فرضية الخيار (أ) سقطت** (المصدر ليس إعداد المتصفح بل القاعدة)، والخيار (ب) يطابق الواقع المحقَّق. **القرار: اعتماد (ب) — أُغلق البند.**

| البند | المشكلة (رقمها في التقرير) | الإجراء المقترح | الملفات | معيار القبول |
|---|---|---|---|---|
| **P1-1** | تناقض قانون الميجريشنز (المؤكد 4) | توحيد SECURITY.md §6/§9.11 مع AGENTS.md §3.3/§6 (التطبيق التلقائي عبر تكامل Supabase–GitHub هو الحقيقة؛ الاستثناء اليدوي = auth.users + ملفات RUN_ON_* القديمة) | `SECURITY.md` | قراءة متتالية للملفين بلا تعارض |
| **P1-2** | قانون الاسم الأعزل (المؤكد 5) | استعادة الأسماء القديمة المحظورة فعليًا في نص القانون: «MusclehubFit» / «MuscleHub» / «Muscle Hub» وأي إملاءات قديمة موثقة في git history (قبل sweep المرحلة 121) | `AGENTS.md` §8 BRAND NAME LAW | النص يحظر أسماء فعلية مختلفة عن «Alkemos» |
| **P1-3** | كسر ترتيب worklog (المؤكد 6) | بأمر مالك: نقل كتلة مراحل 206–211 من أسفل الملف إلى موضعها الزمني الصحيح + رفع مدخل 213 فوق 212 — عملية إعادة ترتيب قانونية واحدة توثق في نفس الكوميت | `worklog.md` | آخر 3 مدخلات = 215/214/213 بترتيب صحيح؛ grep يظهر التسلسل الزمني سليمًا |
| **P1-4** | دورية التوثيق متوقفة (المؤكد 3) | (أ) استئناف الدورية بمدخل `DOC-AUDIT-2026-09-16` في worklog يشير للتقرير؛ (ب) تقوية `scripts/docs_audit.py`: فحص ترتيب worklog (newest-on-top)، فحص «Last updated» مقابل git log، فحص تجميد الأرشيف (git log على archive/) | `worklog.md` · `scripts/docs_audit.py` | البوابة الجديدة ترفض push عند كسر أي فحص جديد؛ التقرير يفشل عمدًا قبل الإصلاح ويمر بعده |
| **P1-5** | سياسة الكاش (المؤكد 2 — الجذر) | **قرار مالك بعد تحقق خارجي** (لوحة Cloudflare): (أ) إن كان Browser-Cache-TTL=5min هو المصدر → ضبطه على Respect Existing Headers والاعتماد على سياسة next.config، أو (ب) اعتماد Cloudflare كطبقة الكاش الرسمية → تحديث توثيق middleware.ts/next.config.ts ليعكسا الواقع + توثيق قاعدة CF في SECURITY.md/TECH_REFERENCE | لوحة CF + `src/middleware.ts` (تعليقات) + `next.config.ts` (توثيق) | القياس الحي بعد الضبط يطابق السياسة الموثقة (أو التوثيق يطابق القياس) — `curl -I` أدلة في worklog |
| **P1-6 🔐** | مقارنة سر `===` (المؤكد 10) | استبدالها بنمط `verifyCronAuth` الثابت زمنيًا (أو تمرير `x-cron-secret` عبر verifyCronAuth معدّل) + توحيد/توثيق `EVO_CRON_SECRET` في SECURITY.md و.env.example | `src/app/api/evo/followup/dispatch/route.ts` · `SECURITY.md` | tsc/eslint/vitest 0 · مراجعة الكود تظهر timing-safe |
| **P1-7 🔐** | zod 3/76 + إدراج غير محدود (المؤكد 11) | سقوف صريحة: `title ≤200` و`result_data ≤10KB` في save-result/save-meal-plan؛ سقوف title/body/link في broadcast (لكن دون كسر سلوك staff المصرح) — موجة zod 2 حسب خطة STATE.md الأصلية | `src/lib/validation/schemas.ts` + المسارين + اختبارات | اختبارات حدود جديدة خضراء؛ طلب > الحد → 400 |
| **P1-8** | `tsc 0` مشروط (المؤكد 12) | الخيار الأنظف: استبدال الاستيرادات النسبية من `public/` في `for-coaches/page.tsx` بمراجع URL نصية عبر next/image (`src="/images/…"` — النمط المعتمد في بقية الموقع) — يزيل الأربعة أخطاء من الجذر ويجعل البوابة مطلقة | `src/app/for-coaches/page.tsx:11-14` | `tsc --noEmit` = 0 من استنساخ نظيف بلا build مسبق |

## WAVE 2 — المرحلة 216: SEO/OG + أداء P2

| البند | المشكلة | الإجراء المقترح | الملفات | معيار القبول |
|---|---|---|---|---|
| **P2-1** | og:image على 14 سطحًا (المؤكد 7) | إضافة `images` للـ14 ملفًا (بطاقات OG موجودة أصلًا في `public/images/og/`) + `twitter.card: "summary_large_image"` حيث ينقص + **توسيع WIRED_SURFACES في `og-image-coverage.test.ts`** ليحرس الملفات الثمانية | الملفات الـ14 المذكورة في التقرير + الاختبار | الاختبار الموسع أخضر؛ فحص حي لـ`og:image` على العينات |
| **P2-2** | EVO header غير لغوي (المؤكد 8) | **بقرار مالك** (يوجد تجميد 2026-09-14): `href={isAr ? "/ar/evo" : "/evo"}` في الموضعين | `src/components/SiteHeader.tsx:304,530` | نقرة AR من الهيدر تهبط `/ar/evo`؛ اختبار إن أمكن |
| **P2-3** | صفحات equipment/muscles ضخمة (المؤكد 9) | حد عرض أولي (60–100 بطاقة) + زر «عرض المزيد» client-side أو تقسيم صفحات؛ الإبقاء على ItemList الـ50 للـ schema | `src/app/equipment/[type]/page.tsx` (+ نظائره muscles/collections لو انطبق) | HTML المُقدَّم ≤150KB مضغوط؛ لا فقدان روابط داخلية (كل التمارين تبقى قابلة للوصول) |
| **P2-4** | JS ~1MB (المؤكد 14) | تحليل حزم (`@next/bundle-analyzer` أو `next build --profile`) لتحديد المكتبات الثقيلة على المسارات العامة وفتح dynamic import حسب النتيجة (recharts وreact-query مرشحان) | يحدده التحليل | خفض ≥25% من نقل JS الأولي على `/` (هدف قابل للمعايرة) |
| **P2-5** | CLS 0.103/0.107 (المؤكد 13) | حجز أبعاد قسم الأدوات (SECTION#tools) قبل الترطيب: أبعاد صريحة للصور/البطاقات أو skeleton بارتفاع ثابت | `src/components/views/LandingView.tsx` (قسم الأدوات) | CLS معملي < 0.05 في EN/AR |
| **P2-6 🔐** | تسريب e.message (المؤكد 16) | رسالة عامة للعميل + log تفصيلي داخلي فقط (قانون AGENTS.md الأصلي: «Never log the AI response in production») | `src/app/api/ai/chat/route.ts:836` · `src/app/api/ai/jobs/route.ts:294` | 500 يرجع رسالة عامة؛ لا تفاصيل مزود |
| **P2-7** | فحص تكوين service key (المؤكد 17) | إضافة بوابة `isSupabaseAdminConfigured` بنمط بقية المسارات | الملفات الأربعة في `api/tools/*` | نقص المتغير → 500 رسالة رشيقة موحدة |

## WAVE 3 — المرحلة 217: نظافة التوثيق الشاملة P3 (دفعة docs واحدة)

| البند | المشكلة | الإجراء |
|---|---|---|
| **P3-1** | ترويسات Last updated خاطئة ×5 (المؤكد 22) | تحديث ترويسات AGENTS/README/DEVELOPER_GUIDE/SECURITY/DESIGN إلى تواريخ git الفعلية + إضافة «المرحلة» الصحيحة |
| **P3-2** | README «6 tools» (المؤكد 21) + env ناقصة (المؤكد 24) | تصحيح الرقم (أو حذف الرقم نهائيًا وفق قانون مصدر الأرقام — المفضل)؛ إضافة `COACH_EMAILS`/`NEXT_PUBLIC_ADSENSE_CLIENT`/`NEXT_PUBLIC_GA_ID` فارغة إلى `.env.example`؛ إدراج NVIDIA في سطر المزودين الأول |
| **P3-3** | SECURITY.md: sin1→fra1 + cron auth + step2 + rate-limit (المؤكد 20) | أربع تصحيحات نصية موثقة أعلاه |
| **P3-4** | STATE.md: السطر اليتيم + سقف 100 (المؤكد 23) | حذف رصاصة مرحلة 192 اليتيمة (المحتوى موجود في سلّم المراحل) + مراجعة ضغط بسيطة تترك ≥3 أسطر هامشًا |
| **P3-5** | TECH_REFERENCE متجمد عند 0069 (المؤكد 24) | تحديث قسم الميجريشنز/RLS حتى 0087 (الأولوية للجداول الحاكمة: ai_plan_usage 0085، evo_* 0077–0081، التدوين الثنائي 0076/0087) |
| **P3-6** | CI_GATES ينقصه workflows (المؤكد 24) | تحديث جدول الـ workflows إلى الـ14 الفعلية |
| **P3-7** | DEVELOPER_GUIDE −7 مسارات + EVO followup غير موثق (المؤكد 24) | استكمال جدول §8 + قسم ميزة follow-up في README (قانون FEATURE README) |
| **P3-8** | أرشيف/قالب worklog (المؤكد 25) | علامة «Deprecated» على ترويسة سياسة «آخر 10»؛ استئناف قالب §12.5.1 حرفيًا في كل مدخل جديد (يُفعّل من مرحلة 215 فصاعدًا)؛ توثيق أن phase213_state_update.py كان سكربتًا محليًا |
| **P3-9** | Breadcrumbs AR (المؤكد 15) | `/ar` بدل `/` في مدخل الرئيسية (blog) + تعريب «Home» (programs) |
| **P3-10** | متفرقات UX/جودة | أرقام مشتقة بدل النصية (م4) · توحيد الأرقام العربية (م5) · وصف memberships EN يكمل Coaching (م6) · LanguageToggle يحفظ query (م8) · safeNext لروابط الإشعارات (توسيع) · og:locale/twitter تناظر · skip-link + label حقل البريد (م26) · تقييم تحويل /chat إلى 301 |
| **P3-11 🔐** | exercise-image بروكسي + receipt_path (المؤكد 18/19) | rate-limit خفيف على المسار + تحقق ملكية مسار الإيصال — بأمر مالك ضمن نفس المرحلة |
| **P3-11 تحديث 2026-09-16 (VERCEL-USAGE-1):** | نصف «exercise-image بروكسي» أُسقط بالكامل — المسار كان ميتًا (صفر مستدعين منذ الدفعة 2 §12.53-2) وحُذف بأمر المالك «فحص و تنظيف استهلاك vercel» بدل الـ rate-limit (نفس فلسفة guard-stale-refs — الحذف أنظف). الباقي من البند: **receipt_path فقط** (تحقق ملكية مسار الإيصال). التفاصيل: docs/VERCEL-USAGE-AUDIT-2026-09-16.md |

## خارج نطاق الخطة (يُدرس بقرار مالك مستقل — ليس عيبًا مؤكدًا)

- إعادة هيكلة i18n إلى `[locale]` routing لإتاحة static generation (الملاحظة م10) — قرار معماري كبير يلغي السبب الجذري لكل صفحات HTML الديناميكية.
- تفعيل CSP الكامل (م1) بعد مراجعة تقارير `/api/csp-report`.
- تفعيل Dependabot + required checks (م14 — عنصران مفتوحان أصلًا في STATE.md).

---

## قاعدة المتابعة

- كل بند يُغلق بذكر رقمه (مثال: «P1-6 ✓») في مدخل worklog المرحلة المعنية + تحديث هذا الملف بحالة البند (منفَّذ/مؤجل/مُسقط بقرار) — البند المُسقط يأخذ سطر «> Dropped (تاريخ): السبب» كما في عرف المشروع.
- الترتيب داخل كل موجة قابل للتعديل بأمر المالك؛ الموجات نفسها مرتبة بالأولوية والأثر.
- هذه الخطة لا تلمس أي ممنوعات نشطة (STATE.md): لا إحياء لأسطح EVO-6 المتقاعدة، لا تعديل ميجريشنز مطبَّقة، لا أرقام متغيرة في README/DEVELOPER_GUIDE.
