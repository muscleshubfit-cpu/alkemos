# جولة تحقق حية مستقلة — إعادة إثبات نتائج UX-TEST-REPORT-2026-09-21

> **التاريخ/الوقت:** 2026-09-21 (17:00–17:20Z تقريبًا) · **الطبيعة:** جولة تحقق حية مستقلة (Fresh Independent Verification Pass) فوق تقرير [`UX-TEST-REPORT-2026-09-21.md`](UX-TEST-REPORT-2026-09-21.md) — جلسة متصفح آلي جديدة بالكامل + SQL قراءة-فقط عبر Management API.
> **حسابات اليوم:** `qa.ux0922.member1` · `qa.ux0922.coach1` · `qa.ux0922.client3` (كلها `@alkemos-test.com` — جديدة، نمط التسمية الموحد).

---

## 1. الهدف

إعادة إثبات أهم نتائج تقرير 2026-09-21 حيًا بجلسة مستقلة جديدة، بأدلة طازجة، وبأدوات مستقلة عن جولة التنفيذ الأولى (لا اعتماد على أي حالة محفوظة منها).

## 2. ما أُعيد التحقق منه حيًا (كل البنود ✅)

| البند | الطريقة | الدليل الحي الطازج (هذه الجولة) |
|---|---|---|
| الرئيسية EN سليمة | متصفح حي | كل الأقسام (أدوات/تمارين/برامج) + صفر أخطاء Console/Errors |
| **BMI E2E** | 82kg/178cm | نتيجة **25.9 · Overweight** (رياضيات صحيحة) + مقياس WHO + الوزن المثالي |
| **i18n ↔ AR/RTL** | زر التبديل | الرابط ينعكس `/tools/bmi-calculator` ↔ `/ar/tools/bmi-calculator` · `dir=rtl` · `lang=ar` · عنوان ومحتوى عربي خالص |
| **تعريب الأطعمة حي** | `/ar/foods/chicken-breast` | «صدور دجاج» في H1 والعنوان |
| **موبايل 390×844** | viewport حقيقي | صفر تجاوز أفقي (scrollW=clientW=390) · 34 عنصر فوتر، أدنى هدف لمس 24px، صفر عنصر <24px (m1 حي) |
| **M1 رسالة الخطأ AR** | دخول بكلمة خاطئة | «البريد الإلكتروني أو كلمة المرور غير صحيحة.» (GoTrue 400 مترجم عربيًا) |
| **M1 رسالة الخطأ EN** | نفس الاختبار بواجهة EN | «Invalid login credentials» (إنجليزية داخل EN) |
| **m9 التسجيل الفوري** | تسجيل عضو جديد | `qa.ux0922.member1` → `/dashboard` فورًا بلا تأكيد بريد |
| لوحة العضو سليمة | تصفح | بطاقات Subscription/Latest weight/Meal plans 0/Workout plans 0 + Quick actions كاملة |
| تسجيل مدرب ذاتي | `/for-coaches/register` | `qa.ux0922.coach1` → `/coach/landing` فورًا · كونسول المدرب كامل |

## 3. 🔴 H1-2026 أُعيد إنتاجه حيًا بالكامل (بأدوات مستقلة)

**السلسلة الحية الطازجة (حساب جديد كليًا عن جولة التقرير):**

1. **الدعوة:** `qa.ux0922.coach1` دعا `qa.ux0922.client3@alkemos-test.com` → `POST /api/coach/clients/invite` ردّ **200** + توست «Invite sent…».
2. **«التسجيل» الفاشل:** جلسة متصفح نظيفة (بلا كوكيز) سجّلت بالبريد المدعو نفسه وكلمة مرور جديدة `Vc#0922Invited!Pass` → الرد: **«Account already exists — Sign in with your password to get straight back in»** + زر «Go to login».
3. **الطريق المسدود:** العميل اتبع النصح ودخل بالكلمة التي اختارها **قبل ثوانٍ** → **«Invalid login credentials»** (GoTrue `grant_type=password` ردّ **400**).
4. **الدليل القاطع SQL (قراءة-فقط، Management API):**

```json
// auth.users WHERE email = 'qa.ux0922.client3@alkemos-test.com'
{"created_at":"2026-09-21 17:00:10.819628+00",   // لحظة الدعوة، قبل أي تسجيل من العميل
 "last_sign_in_at":null,                          // لا جلسة قط
 "pw_is_empty":true,                              // encrypted_password='' رغم اختيار العميل كلمة مرور
 "email_confirmed":true}
```

```json
// coach_assignments JOIN — الإسناد موجود لحظة الدعوة
{"id":"6682f0f8-d109-467c-95d0-c0b325ac25b5","created_at":"2026-09-21","coach_name":"QA UX Verify Coach"}
```

**الاستنتاج:** خلل H1-2026 (حساب الظل بكلمة مرور فارغة من `inviteUserByEmail` → العميل المدعو محبوس بين «Account already exists» و«Invalid login credentials» بلا مخرج ذاتي إلا رابط الدعوة الإيميلي) **ما زال حيًا على الإنتاج** — الحلول الثلاثة المقترحة في §3 من التقرير الرئيسي تبقى قائمة كما هي.

## 4. ملاحظة جديدة صغيرة (m-D — تناقض عدّادات قائمة العملاء)

في لوحة المدرب `qa.ux0922.coach1` بعد الدعوة مباشرة: **«Total clients: 0»** بينما القائمة نفسها تعرض صفّ العميل المدعو وفلتر **«No subscription: 1»** — ثلاث قراءات متضاربة في شاشة واحدة (العداد الكلي = 0، الفلتر = 1، الصف ظاهر). أثرها ثقة المدرب بعدّاداته. **الحل المقترح:** توحيد مصدر العد (القائمة هي المرجع) أو تمييز «الدعوات المعلقة» عن «العملاء» في كلٍّ من العداد والفلتر (يتكامل مع فرصة التحسين 1 وقائمة الدعوات المعلقة m-C).

## 5. ملاحظة توست مكررة (احتكاك بسيط)

رسالة خطأ الدخول تُعرض **مرتين** في نفس اللحظة (توستان متطابقان — لوحظ في كل محاولات M1 بغتين). غير عائق، لكنها تُفقر الإحساس بالصقل. **الحل:** منع التكرار بنفس المحتوى خلال نافذة قصيرة (dedupe في مدير التوست).

## 6. حدود الجولة

- جولة تحقق مركزة: أعادت إثبات **الأدوار الزائرة (Guest/Free/Coach) + التسجيل + H1-2026** بأدلة طازجة مستقلة؛ لم تُعد تشغيل مسار المال الوهمي E2E (موثق بالأمس في التقرير الرئيسي §4 بتفصيله الكامل — لم يتغير أي كود بينهما: آخر كوميتات `main` كلها `[vercel skip]` docs-only فلا نشر جديد).
- Google OAuth (الزر حاضر وحي في الشاشتين بغتين) وبريد الإيميل الفعلي يظلان بيد المالك كما هوثق §6 من التقرير الرئيسي.

## 7. الخلاصة

**نتائج تقرير 2026-09-21 صامدة بالكامل تحت إعادة الفحص المستقل:** 12/12 إصلاحًا سابقًا ما زالت حية (عينة اليوم من m1/m3-الفحص غير المطلوب/M1/m4/m6/m9)، وH1-2026 حي ومُثبت بدليل SQL قاطع، وملاحظتان جديدتان خفيفتان (m-D تناقض العدّادات + التوست المكرر) تُضاف لقائمة البنود الصغيرة.
