# Alkemos — Comprehensive Sports Platform

> **Live:** [alkemos.com](https://alkemos.com)
> **Repository:** [github.com/muscleshubfit-cpu/alkemos](https://github.com/muscleshubfit-cpu/alkemos)
> **Stack:** Next.js 16 · React 19 · Supabase · OpenRouter + Groq + NVIDIA NIM (AI) · Tailwind CSS 4
> **CI Gates:** every push is audited — schema-drift · registry-parity · knowledge-system · anti-regression
> **Last updated:** 2026-09-19 (Phase 237 — migration Phase 5: worklog entry description updated for the rotation — the live file is now the active window + rolling buffer, full history in `archive/WORKLOG_ARCHIVE.md`; earlier today Phase 236 added the `docs/README.md` index; for current live status always refer to [`STATE.md`](./STATE.md))

## 🏷️ Brand Rename — Musclehubeg → Alkemos (Phase 121)

The platform was rebranded to **Alkemos** when the owner bought
[alkemos.com](https://alkemos.com) and made it the production domain. What
the rename covers and how it works:

- **Where it lives:** every user-visible brand string in the app comes from
  the code itself (UI copy in `src/**`, metadata in `src/app/metadata.ts`,
  i18n brand keys in `src/lib/i18n.tsx`, blog generation prompts in the
  pipeline modules (`src/lib/blog-pipeline.ts` · `src/lib/blog-research.ts`)
  and the coach draft generator (`src/lib/ai-job-processors.ts`),
  affiliate banners in `public/affiliate/*.svg`,
  PWA identity in `public/manifest.json`) — all now say Alkemos.
- **Visual assets:** `public/logo.png` (header + OG), the app icons
  (`icon-512/192`, `apple-touch-icon`, `favicon.png/.ico`) and the animated
  badge `public/logo.svg` were regenerated with the same design language
  (black background, metallic silver + electric blue, bold italic
  wordmark, `TRAIN. FUEL. TRANSFORM.` tagline) around a new **A monogram
  with a barbell crossbar**. Generated by script (cairosvg), never by hand.
- **Existing data:** migration `supabase/migrations/20260906120000_0070_rebrand_alkemos.sql`
  renames the old brand inside existing rows (blog articles, notifications,
  coach pages, plan titles) and moves `blog_posts.author` default from
  `MuscleHub` to `Alkemos`. It is data-only, idempotent, and never touches
  `slug` columns (URL stability) or `auth.users`.
- **Functional identifiers are intentionally NOT renamed:** the GitHub repo
  path (`muscleshubfit-cpu/alkemos`), the private backup repo
  (`musclehubeg-backups`), `project_id` in `supabase/config.toml`, and the
  contact emails — renaming any of them would break integrations, the
  daily DB-backup job, or the Supabase–GitHub link.
- **Old links keep working:** `musclehubeg.vercel.app` → 301 → `alkemos.com`
  (do not rename the Vercel project or that redirect dies).

A bilingual (Arabic/English) fitness & nutrition platform with 868 exercises, 8,830 foods, a workout-programs library, free tools, an AI coach (EVO) with live streaming chat and a two-window plan-quota system, a public coach directory, an automated bilingual blog CMS, a full B2B coach system (clients, wallets, activations, certificates, public coach pages), a B2C site-coach follow-up layer (`coach_kind` + `site_coach_assignments`), Admin Panel 2.0 (unified client roster with type filters, finances, site-assignments), an affiliate program with 20% commissions, a 7-day conditional refund system, and membership tiers. The engineering is gated too: automated quality gates (schema-drift, registry-parity, knowledge-system, anti-regression) audit every push on GitHub Actions — see the 🛡️ section below. <!-- P3-2 (Phase 217): the tools count is a variable number — it lives in `src/lib/tools-shared.ts` (TOOLS_COUNT) only, per the single-source number law. -->

---

## ⚠️ Public Repository ≠ Open-Source Product

This repository is **public for development transparency, agent-assisted
collaboration, and auditing** — but the product is **proprietary**. The
code is NOT open source.

- You may read, study, and discuss the code for educational purposes.
- You may submit issues / pull requests back to this repository.
- You may **NOT** redistribute the code, deploy a competing service,
  remove the license notice, or use it to train commercial
  code-generation models.

Production infrastructure (Supabase project, OpenRouter keys, Vercel
deployment), customer data, financial data, and confidential business
information are **private** and never appear in this repository.

See [`LICENSE`](./LICENSE) for the full proprietary terms and
[`SECURITY.md`](./SECURITY.md) for the security policy.

---

## 🛡️ Automated Quality Gates — Every Push Is Audited

Every push and pull request is audited on GitHub Actions by permanent gates that re-derive the truth from the code itself and fail loudly on any lie — a drifted schema mirror, a rotted doc, a resurrected retired identifier, or a migration that breaks the database — and a failing gate blocks the merge: schema-drift audit · registry parity · knowledge-system audit · anti-regression guard · Supabase preview migrations.
The gates live in the repo itself (`.github/workflows/` + `scripts/`) so they travel automatically with any future clean copy or rebrand, and the current project state is always [`STATE.md`](./STATE.md).
**للاطلاع على التفاصيل الكاملة** — قصة كل بوابة والحادثة اللي بنتها · سجل الـworkflows · أوامر التحقق القانونية: [`docs/CI_GATES.md`](./docs/CI_GATES.md).

---

## 🚀 Quick Start

### Prerequisites

- **Node.js** 18+ (or **Bun** 1.3+)
- **Supabase** project (free tier works)
- **OpenRouter** API key (free tier works — uses free models)

### Installation

```bash
# Clone the repo
git clone https://github.com/muscleshubfit-cpu/alkemos.git
cd alkemos

# Install dependencies
bun install
# or: npm install

# Copy env file and fill in your keys
cp .env.example .env.local
```

### Environment Variables

Create `.env.local` with the following (see `.env.example` for full reference):

```env
# === Required for production ===
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# === AI (OpenRouter + Groq + NVIDIA NIM — owner directive 2026-08-27, NVIDIA added 2026-09-09) ===
OPENROUTER_API=sk-or-v1-xxxxxxxxxxxxx
GROQ_API_KEY=gsk_xxxxxxxxxxxxx
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxx

# === Optional integrations ===
NEXT_PUBLIC_ADSENSE_CLIENT=ca-pub-xxxxxxxxxxxx
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_SITE_URL=https://your-domain.com
COACH_EMAILS=coach@example.com,admin@example.com
CRON_SECRET=your-cron-secret

# === Optional: distributed rate limiting (Upstash Redis free tier) ===
# When set, public API endpoints (lead capture, email, coach register)
# share fixed-window rate-limit counters across ALL serverless
# instances via Upstash's REST API. Without these, limiting falls back
# to a per-instance in-memory Map (resets on cold starts).
# Console: https://console.upstash.com → Create Database → REST API section
UPSTASH_REDIS_REST_URL=https://your-cache.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-rest-token
```

### Database Setup

`supabase/migrations/` holds the versioned migration registry in four
naming families (see [`supabase/migrations/INDEX.md`](./supabase/migrations/INDEX.md) — the
binding registry). The numbering map lives in INDEX.md — it is the
single documented home of the registry range:

| Family | Example | Applied by |
|---|---|---|
| `0001_*.sql` (numeric only) | `0001_init.sql` … `0005_blog_generation_queue.sql` | Supabase GitHub integration (automatic) |
| `YYYYMMDDHHMMSS_NNNN_*.sql` (dated) | `20260902120000_0063_schema_drift_backfill.sql` … `20260903153000_0068_admin_client_type_fix.sql` | Supabase GitHub integration (automatic) — **the required format for all new migrations** |
| `RUN_ON_SUPABASE_*` | `RUN_ON_SUPABASE_0066_DELETE_TEST_ADMIN_ACCOUNT.sql` | Manual only (SQL Editor) — never auto-applied |
| `VERIFY_*.sql` | `VERIFY_SCHEMA_DRIFT.sql` | Manual, read-only verification scripts |

For a fresh project: run the numeric family first, then the dated
family in timestamp order, then
`NOTIFY pgrst, 'reload schema';` to refresh the PostgREST schema cache —
or just `supabase db push` with the CLI. Manual `RUN_ON_SUPABASE_*`
scripts are applied only when the corresponding legacy feature is needed.

> **Historical drift — RESOLVED (Phase 99-run, 2026-09-03):** three
> tables created ad-hoc on production during Phase 5 (`plan_swaps`,
> `progress_photos`, `coach_presence`) were back-filled into migration
> `0063_schema_drift_backfill.sql`, RLS-hardened in `0064`
> (progress_photos) and `0065` (plan_swaps), and the stalled pipeline
> (0064→0067) was unblocked and live-verified. A first failed migration
> rolls back atomically and HALTS the auto pipeline — that is by design
> (loud failure over silent drift; see the 0054 ledger repair).

### Run Locally

```bash
bun dev
# or: npm run dev
```

Open [http://localhost:3000](http://localhost:3000)

### Demo Mode

If Supabase env vars are NOT set, the app runs in **demo mode** (localStorage-backed):
- Demo coach: `coach@coach.app` / `coach123`
- Demo client: `client@demo.app` / `client123`

---

## 📦 Deployment (Vercel)

1. Push your code to GitHub
2. Go to [vercel.com](https://vercel.com) → Import Project → select your repo
3. Add all environment variables in Vercel → Settings → Environment Variables
4. Deploy

The app is configured for Vercel with (see [`vercel.json`](./vercel.json)):
- **Framework:** Next.js
- **Region:** Singapore (sin1)
- **Install command:** `bun install`
- **Build command:** `next build` (production — vercel.json override)
- **Security headers:** HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy
- **Caching:** 1-year immutable for `/_next/static` and `/images/*`
- **Cron jobs (2):** weekly progress reminder (Sunday 07:00 UTC) + daily `dispatch-pipelines` (23:40 UTC — tops up genuinely missed blog slots + rescues the AI-jobs worker; Phase 171: 90-min grace per slot so GitHub's scheduler delay is never mistaken for a missed slot, coverage counts actual published posts, and P5 refuses a second automated article the same day)
- **Blog cadence:** 2 articles/day — 1 EN (`blog-post-en.yml` at 22:00 UTC = 18:00 US Eastern) + 1 AR (`blog-post-ar.yml` at 05:00 UTC = 08:00 Cairo EEST), at different times per audience geography (Phase 119 owner directive 2026-09-04); ONE workflow run == ONE article in ONE language, the dispatcher only tops up missed slots (never exceeds 1+1), and the one-automated-article/day/language law is enforced at the P5 publish layer itself (Phase 171 — coach-requested articles are exempt owner overrides)

**Database migrations on deploy:** the Supabase GitHub integration
auto-applies every auto-named migration (numeric + dated families) on
push, in order. One failing migration halts the whole pipeline until
fixed — proven live in Phase 99-run, where a single 42703 on two phantom
columns froze the ledger at 0063 and silently held back 0064→0067
(RLS hardening + the entire Phase 103 admin unification) until corrected.

**Automatic deployment cleanup (Phase 145, owner directive 2026-09-08):**
every push to `main` creates a Vercel deployment, and each retained
deployment holds its serverless "Function Storage" bundles — on the free
plan that quota (10 GB) filled to 100% in under a week and blocked new
deploys (incident `VERCEL-FS-CLEANUP-2026-09-08`, purged live by the
agent). The daily scheduled workflow
[`.github/workflows/vercel-cleanup.yml`](./.github/workflows/vercel-cleanup.yml)
(01:00 UTC) runs
[`scripts/vercel-cleanup/vercel-cleanup.mjs`](./scripts/vercel-cleanup/vercel-cleanup.mjs)
(a zero-dependency Node script) to purge stale READY deployments while
ALWAYS keeping: the current production deployment (the one holding the
alkemos.com aliases), everything newer than the freshness window
(`KEEP_HOURS`, default 48h), and the newest preview deployments
(`KEEP_PREVIEWS`, default 2). Superseded deployments stay reproducible
from git at any time. Requires the repository secret `VERCEL_TOKEN`
(Settings ▸ Secrets and variables ▸ Actions — a project-scoped Vercel
token is enough); until it exists the preflight step fails fast naming
the fix. Plan-only inspection: Actions ▸ Vercel cleanup ▸ Run workflow
▸ tick DRY RUN.

---

## 🏗️ Project Structure

```
alkemos/
├── src/
│   ├── app/                    # Next.js App Router (page count = code truth)
│   │   ├── (app)/              # Authenticated routes (dashboard, coach, plans, progress…)
│   │   ├── (home)/             # Landing page route group
│   │   ├── admin/              # ADMIN-ONLY — Admin Panel 2.0 with dedicated AdminShell
│   │   │                       #   (AdminGate redirects coaches to /coach; APIs return 403).
│   │   │                       #   Sections: dashboard (KPIs + quick actions, /admin redirects
│   │   │                       #   here), clients (UNIFIED roster — type filter buttons
│   │   │                       #   الكل/أعضاء الموقع/عملاء B2B/مدربو الموقع/مدربو B2B/الإدارة +
│   │   │                       #   lifecycle tabs + danger tools), site-assignments (B2C
│   │   │                       #   member↔site-coach 1↔1), coaches (real roster + one-tap
│   │   │                       #   site/B2B kind toggle), finances (SITE money B2C vs COACH
│   │   │                       #   money B2B), payments (+7-day refund console), members +
│   │   │                       #   accounts (legacy redirects), assignments, blog CMS,
│   │   │                       #   coach-pages, coach-support, coach-system, referrals,
│   │   │                       #   leads, saved-results, wallets, external-plans
│   │   ├── api/                # API endpoints — see DEVELOPER_GUIDE §8 (count = code truth)
│   │   ├── ar/                 # Full Arabic RTL mirror (blog, exercises, foods, programs,
│   │   │                       #   coaches, for-coaches, memberships, faq…)
│   │   ├── affiliate/          # Affiliate program page (7-step program, 20% commission)
│   │   ├── blog/               # Blog (list + article + OG images)
│   │   ├── checkout/           # Checkout flow
│   │   ├── coaching/           # Human coaching marketing page
│   │   ├── coaches/[slug]/     # Public coach directory pages (+ featured API + preview route)
│   │   ├── evo/                # EVO AI coach marketing page
│   │   ├── exercises/          # Exercise library (868)
│   │   ├── foods/              # Food database (8,830)
│   │   ├── for-coaches/        # Coach join funnel (+ register)
│   │   ├── meal-planner/       # Interactive meal planning tool
│   │   ├── memberships/        # Pricing + membership tiers + comparison table
│   │   ├── programs/           # Workout programs library (7 programs)
│   │   ├── tools/              # 5 calculators (BMI, body fat, calories, macros, water)
│   │   ├── profile/            # User profile + settings
│   │   ├── layout.tsx          # Root layout (providers, analytics, ads, PWA)
│   │   └── metadata.ts         # Site-wide SEO metadata
│   ├── components/
│   │   ├── ui/                 # shadcn/ui primitives (new-york style)
│   │   ├── views/              # Page-level views
│   │   ├── admin/              # AdminShell (sidebar + mobile button grid + live badges)
│   │   │                       #   + admin/ui.tsx shared primitives (StatTile, badges, tabs…)
│   │   ├── blog/               # Blog article + list components
│   │   ├── coach/              # Coach landing content
│   │   ├── SiteHeader.tsx      # Navigation + auth + notifications
│   │   ├── AppLayout.tsx       # Role sidebar (member/coach — admins live in /admin)
│   │   ├── EvoFloatingWidget.tsx # EVO AI chat floating widget (quota meters)
│   │   ├── AdminNotificationBell.tsx / NotificationBell.tsx
│   │   ├── SaveResultButton.tsx # Tool result save + PDF export
│   │   └── ...
│   ├── hooks/                   # React hooks (auth, nav, tier, mobile, toast, scroll, voice)
│   ├── lib/                    # Business logic + data layer
│   │   ├── memberships.ts      # Tier definitions + pricing + limits (incl. the unified monthly AI-plan pool 2/4/8/8)
│   │   ├── tier-limits.ts      # Quota engine: chat/swaps + the unified monthly plan pool
│   │   │                       #   (checkUnifiedPlanQuota — success-only via ai_plan_usage, Phase 183)
│   │   │                       #   + hashGuestKey/hashIpKey (G6 dual-dimension guest identity:
│   │   │                       #   used = max(browser, network) — incognito can't reset the pool)
│   │   │                       #   — per-kind/weekly fns retired
│   │   ├── coach-limits.ts     # B2B pricing + activation fees
│   │   ├── data/               # Data layer — 13 modules (index re-exports auth, blog, chat, coach,
│   │   │                       #   notifications, plans, progress, questionnaires, referrals,
│   │   │                       #   subscriptions, tickets + helpers; Supabase OR localStorage demo)
│   │   ├── plan-generator.ts   # AI nutrition/workout plan generation + meal/food-item/day
│   │   │                       #   regeneration + exercise substitution
│   │   ├── external-plan-text.ts # Structured non-member plan types + Arabic text rendering
│   │   ├── ai-jobs.ts          # Queued AI job types + staff gates (JOB_GATE) + payload sanitization
│   │   ├── ai-job-processors.ts # GHA-runner job processors + draft materialization
│   │   ├── refund.ts           # 7-day refund eligibility (no-features-used, ledger-based)
│   │   ├── affiliate-engine.ts # Affiliate engine + COMMISSION_RATE 20% + payout holds
│   │   ├── auth-server.ts      # Server auth helpers (requireUser, requireCoach, requireAdmin)
│   │   ├── ai-provider.ts      # Unified AI layer: OpenRouter + Groq + NVIDIA NIM ONLY (callAI, callAIWithFallback,
│   │   │                       #   callFreeOpenRouterRace, callFreeAIFallbackChain, callAIStream; ≤52s clamp)
│   │   ├── exercises.ts        # 868 exercises dataset (yuhonas MIT-licensed)
│   │   ├── (images self-hosted: public/images/exercises/*.webp — batch 2, immutable long-cache)
│   │   ├── foods.ts            # 8,830 foods dataset
│   │   ├── workout-programs.ts # Workout programs library content
│   │   ├── blog-*.ts           # Blog generation pipeline (research → outline → content → images → publish)
│   │   ├── evo-chat-context.tsx # EVO chat state management
│   │   ├── referral.ts + referral-cookie.ts # Referral + commission system
│   │   ├── result-png-export.ts # Canvas → PDF/PNG export
│   │   ├── seo.ts              # JSON-LD schema generators
│   │   ├── supabase/           # Supabase clients (client.ts, admin.ts, types.ts)
│   │   └── ...
│   ├── middleware.ts            # Session refresh + Content-Language header + locale-aware lang/dir
├── supabase/
│   └── migrations/             # Versioned migration registry (INDEX.md is binding; manual
│                               #   RUN_ON_SUPABASE_* + VERIFY_* scripts included)
├── scripts/                    # ai-jobs-runner (GHA), blog-runner (GHA), migration_audit.py
│                               #   (schema-drift gate, Phase 96), docs_parity.py (registry
│                               #   gate, Phase 106), docs_audit.py (knowledge gate, Phase 107),
│                               #   check-stale-refs.sh
├── public/                     # Static assets (icons, QR codes, images, manifest, sw.js)
├── .github/workflows/          # blog-post-ar, blog-post-en, process-ai-jobs,
│                               #   remediate-blog-images, guard-stale-refs, docs-parity-gate
├── archive/                     # أرشيف المراحل السابقة والملفات المدمجة (PROGRESS, QA_CHECKLIST)
├── AGENTS.md                   # AI agent operating rules
├── SECURITY.md                 # Security policy
├── LICENSE                     # Proprietary, all rights reserved
├── STATE.md                    # THE single official live state (merged status law — Phase 115)
├── CONTRIBUTING.md             # Contribution policy (proprietary — issues only)
├── DEVELOPER_GUIDE.md          # Developer onboarding + architecture details
│                               #   (status history frozen in archive/ since Phase 115)
└── package.json
```

---

## 🎯 Key Features

### For Users
- **Social Sharing unification + OG metadata gaps closed (Phase 231, 2026-09-18 — owner order «نفّذ الآن جميع إصلاحات Social Sharing المتبقية من تقرير Deep Audit، كمرحلة واحدة بعد إغلاق Phase 230» — display/metadata layer only, zero business-logic/API/DB/Cloudflare changes):** the three public share components are now thin wrappers over ONE implementation — `buildShareLinks()` in `src/lib/share-links.ts` is the ONLY place a share-intent URL is built (payload formats verbatim, platforms are a per-surface CONFIG: ShareButtons = WhatsApp/FB/X/LinkedIn/Telegram, blog `SocialShare` = FB/LinkedIn/X/WhatsApp — no Telegram, `CoachShareButtons` = FB/X/Telegram — the «معادا زر واتساب لن نضيفها» decree preserved) and `useShareActions` (`src/components/share/useShareActions.ts`) is the single copy-with-fallback + post-mount Web Share behavior (removes the blog bar's latent render-time hydration probe). Every surface passes its canonical path to `canonicalShareUrl()` (the Phase-230 single source) — blog article shares and the for-coaches bar included; `window.location`/`mountedUrl` are retired from every share component. A share-COVERAGE guard (`share-unify.test.tsx`) scans all of `src/` and FAILS CI on any unregistered share surface (15 registered) or any hand-rolled intent URL. OG metadata: /ar gained its og:url; /blog and /ar/blog own independent index metadata (og:title/description/url/image — no longer the homepage card); EVO EN+AR got dedicated 1200×630 cards (og-evo-en/ar); the for-coaches pair swapped the vertical 1122×1402 coach-portrait for horizontal branded 1200×630 cards (og-for-coaches-en/ar); the blog index got dedicated cards (og-blog-en/ar) — six new cards via the sanctioned `scripts/generate-og-cards.py` (20 total). Programs/Coaching/Memberships were reviewed and left as-is (their og:url/cards are correct and documented). Where: `src/lib/share-links.ts`, `src/components/share/useShareActions.ts`, `src/components/ShareButtons.tsx`, `src/components/blog/BlogComponents.tsx` (SocialShare), `src/components/CoachShareButtons.tsx`, `src/app/for-coaches/page.tsx`, `src/components/blog/BlogArticlePage.tsx`, the ar/blog + evo + for-coaches layout/page metadata files, `scripts/generate-og-cards.py` + `public/images/og/`.
- **Social Sharing P0 — deterministic canonical share URLs (Phase 230, 2026-09-18 — owner order «نفّذ الآن P0 بالكامل من تقرير Deep Audit لنظام Social Sharing» — display-layer only, zero business-logic/API/DB/metadata changes):** every `ShareButtons` surface (EVO, the five tools, exercise/food/program details, meal-planner, coaching, memberships — 12 usages, EN+AR) now builds its share URL from a REQUIRED `path` prop via the single source `canonicalShareUrl()` in `src/lib/share-url.ts`: the absolute canonical URL is complete from the FIRST server render (the server previously emitted empty share hrefs — WhatsApp shared text with no link, the Facebook/LinkedIn popups flashed and failed), `window.location` is gone from the component entirely (no pre-hydration empty-href window), and query/hash params (`utm_*`, `cb`) are structurally stripped so trackers can never leak into a share. The Web Share button renders only when `navigator.share` exists (the always-visible Copy-link button is the explicit fallback on desktops without the API). Platforms, message payloads, labels and UI are unchanged (this scope law was superseded by Phase 231, which unified blog `SocialShare` and `CoachShareButtons` over the same engine — see the Phase-231 entry above). Guarded by `src/lib/__tests__/share-url.test.tsx` (18 tests: SSR render has zero empty hrefs on EN+AR, no leakage, Web Share conditional, source guards over all 12 surfaces). Where: `src/lib/share-url.ts`, `src/components/ShareButtons.tsx` + the 12 usage files.
- **Deep-UX audit fixes (Phase 227, 2026-09-18 — display-layer only, zero business-logic/API/DB changes):** (1) **Share buttons repaired + hydration #418 closed** — the share components used to read `window.location` during render, which failed hydration on every public page that renders them AND left the live share links with an empty URL (React does not patch mismatched attributes); the URL is now resolved after mount, so sharing actually works. (2) **Server errors render in the UI's language at the worst moments** (login failures, wallet-gated activation): `src/lib/error-i18n.ts` maps the stable error codes (+ numeric payload) at the display layer — the Arabic server messages stay the single source for Arabic, unknown errors pass through verbatim. (3) **Mobile footer touch targets ≥24px** (WCAG 2.5.8 AA): social anchors are 32×32 hit areas and every footer link is a full-width 24px-tall tappable row. (4) **EVO composer is full-bleed on phones** (drawer edge-to-edge below `sm:`, desktop unchanged) with a visibly wider input — the 40px mic/send touch targets untouched. (5) **Questionnaires render friendly labels for enum-ish answers** (`src/lib/questionnaire-display.ts`, member review + coach card): known tokens like `lose_fat`/`beginner`/`dumbbells` display bilingually; genuine free text passes through verbatim. (6) **The plans page explains the Free swap boundary** instead of a broken-looking "0/0 remaining" — an honest "Premium feature" note with the memberships link. (7) **Signup shows a live password-strength meter + guidance** (display-only — the enforced gates remain the 8-char minimum + the HIBP breach check). Where: `src/components/ShareButtons.tsx`, `src/components/CoachShareButtons.tsx`, `src/lib/error-i18n.ts`, `src/components/SiteFooter.tsx`, `src/components/EvoFloatingWidget.tsx`, `src/lib/questionnaire-display.ts`, `src/components/views/QuestionnairesView.tsx`, `src/components/views/CoachClientView.tsx`, `src/components/views/PlansView.tsx`, `src/lib/password-strength.ts`, `src/components/views/AuthView.tsx` (guarded by 24 new unit tests)
- **Deep-audit WAVE 2 (Phase 216, 2026-09-16 — SEO/OG + performance pass, zero pricing/quota/API logic changes):** (1) **Equipment & muscle-group hubs render a capped grid** (`/equipment/*`, `/muscles/*` + AR mirrors): the first 60 exercise cards are server-rendered and the rest arrive via one «Show more exercises / عرض المزيد» button (`src/components/hubs/ShowMoreExercises.tsx` + the server-safe constant in `hub-exercises-shared.ts`) — every exercise stays reachable, the ItemList schema keeps its top-50 slice, and the heaviest hub (`/equipment/bodyweight`, 348 cards) drops from ~550KB to ~213KB of served HTML (~61% less to parse on mobile). (2) **og:image social cards on every public surface** — 17 files that declared their own `openGraph` block without images (Next.js replace-not-inherit) now pin their family card (home/tools) + `summary_large_image`, including the FAQ/compare/planner/register pages in both languages and three same-class discoveries (meal-planner pair + AR equipment); guarded by the extended `og-image-coverage.test.ts`. (3) **The header EVO link is language-aware** (`/ar/evo` in Arabic — owner decision 2026-09-16 supersedes the 2026-09-14 freeze), matching the logo and footer. (4) **CLS fixed on the Arabic homepage** (0.107 → ~0.01 lab): a new `"Arabic Fallback"` @font-face with Cairo's exact metric overrides stops the pre-swap text from reflowing the page (the automatic next/font fallback is `local(Arial)` — which has no Arabic glyphs). (5) **The EVO chat UI (widget + provider + voice input, ~109KB) now loads only after the page's `load` event** — eager CTAs open it through the zero-dependency `openEvoFloatingChat` dispatcher in `src/lib/evo-chat-events.ts`. (6) 500 responses from `/api/ai/chat` and `/api/ai/jobs` return a generic message (no internal/provider detail — owner §7 approval 2026-09-16), and the four `/api/tools/*` routes answer a graceful "Server not configured" 500 when the service key is missing.
- **Homepage (Phase 202, owner order 2026-09-15 «Homepage حقيقية لموقع Fitness Platform هدفه الانتشار والاستخدام، وليس البيع»):** a PRODUCT-WEBSITE homepage where every service family is a real CONTENT ENTRY POINT — Hero (simple intro + the account-action CTA pair — signup/login for guests, the member console when signed in — refined in Phase 203 below) → Tools & AI (the AI-plan lead card + a compact tools grid) → Training (8 REAL exercise cards with images/level/equipment + muscle-group chips with live counts + 3 REAL programs with images/splits/duration) → Nutrition (8 REAL foods with per-100g calories & macros) → Articles (the blog carousel as its own visible section) → one brief Coaching section (no prices, one explore CTA) → FAQ (the usage questions — five since Phase 203) → footer. The server route pages pick the curated real samples from the live libraries (`src/lib/home-samples.ts` — bundle law holds: the giant arrays never ship to the browser) and pass them to the client `LandingView` as props; the OAuth-error toast moved to the `AuthErrorToast` island. RETIRED from the homepage per the owner order: the Memberships tiers section + price tables + comparison table + closing sales CTA (the `/memberships` page and all pricing/quotas are untouched — reachable from the drawer, the footer, and coaching), and the EVO promotional card (the floating EVO widget is the access point). Header: visible DESKTOP navigation for the five core services (Training / Nutrition / Tools / AI / Coaching — dropdowns open on hover, keyboard focus, and click) with secondary services (Memberships / Affiliate / For Coaches) demoted to the drawer's «More» group; the drawer itself is service-first. Footer: the ecosystem SERVICE MAP (Training / Nutrition / Tools & AI / Coaching & Services / Company — every pre-existing link kept) with the owner's new tagline («Built with care for the fitness community» / «صُنع بحب لمجتمع اللياقة»). Display-only redesign — no service, route, price, quota, or API changed — where: `src/components/views/LandingView.tsx`, `src/components/SiteHeader.tsx`, `src/components/SiteFooter.tsx`, `src/lib/home-samples.ts`, `src/app/(home)/page.tsx`, `src/app/ar/page.tsx`, `src/components/AuthErrorToast.tsx` (guarded by `src/lib/__tests__/homepage-adoption.test.ts`)
- **Final internal copy audit (Phase 205, owner order 2026-09-15 «Final Internal Copy Audit» — deep READ/WRITE audit of every internal page AR/EN except the blog; copy-only, zero functionality/API/DB/quotas/pricing changes):** (1) **CJK machine-translation contamination purged:** 23 Chinese splices inside Arabic exercise instructions (`exercises.ts` — «将有助于恢复/拉伸/面向/滚动/远离/以便/而是/或在/放松/挂在/引き/几次/后退/立正» and friends) replaced with natural MSA translated from each instruction's EN twin (e.g. «Continue to descend to the bottom squat position, which will help in the recovery» → «…مما سيساعد على الاستشفاء»); a new guard `src/lib/__tests__/no-cjk-contamination.test.ts` walks every non-blog source file's string literals and fails on ANY CJK char (the blog's own sanitizer route/tests are the sanctioned exceptions). (2) **Residual terminology/dialect cleanup:** «الكارب» ×8 in the collections hub (the low-carb collection's title/H1/intro/description + high-protein intro/description + keto description) → «الكربوهيدرات» (+2 spelling fixes «كاربوهيدرات»→«كربوهيدرات»); «الجيم» ×3 + «الصالة» ×5 in the collections/equipment depth guides → «النادي الرياضي» (the Phase-204 unified term — incl. «تُجوّع الجيم»→«تُجوّع تدريبك», «كما يفعل أي أحد»→«كما يفعل أي شخص آخر»); «التونا» ×21→«التونة» and «سمك التلبية» ×5→«سمك البلطي» (verified against the food DB's nameAr); «الأكلات/أكلة/أي حاجة رياضية» in the EVO floating widget, `seo.ts` site description, and the AR foods mirror (metadata + breadcrumb JSON-LD) → «الأطعمة/صنف غذائي/أي موضوع يخص اللياقة»; app-surface strings: the PlansView swap-failure toast («هتحتاج تعمله تاني»→«ستحتاج إلى إعادته»), the printed-plan brand tag («كوتش أونلاين»→«مدرب أونلاين») and its «كارب» macro label, CheckoutView's manual-payment note («موافقة الكوتش»→«موافقة المدرب»), CoachView's empty-search label («مفيش نتايج»→«لا توجد نتائج مطابقة»), the two visible strings in `ai-local.ts` plan fallbacks and `external-plan-text.ts`. (3) **Title unification:** the AR foods page title is now «قاعدة بيانات الأطعمة» (no double branding — the /ar layout template already appends «— Alkemos») with an MSA description carrying the real «8,830+ صنف غذائي». (4) **Verified clean (no change needed):** competitor prices in `comparisons.ts` are correct external facts; affiliate commissions are exactly 20% of the live prices + $10 minimum payout; 8 tools / 868+ exercises / 8,830+ foods / 2-4-8-8 generations / 10 EVO messages / 3-6 swaps all match the truth sources verbatim; «كوتشينج بشري» is the approved Phase-204 term (EN twin «Human coaching»). (5) **Guards:** the marketing-MSA manifest gained 9 files (hub-collections + the three hub-depth modules + hub-depth + ar/foods page + food detail + external-plan-text + seo.ts) and a Phase-205 describe pins every removed phrase dead + the unified replacements present. Gates: tsc 0 · eslint 0/0 · vitest 1175/1175 · build 0 · live EN/AR desktop+mobile QA with zero overflow. **Documented out-of-scope findings (not expanded):** the «كارب» macro chip on the homepage (`LandingView` — a Phase-203-closed surface), the dead-code `generateChatReply` in `ai-local.ts` (full-Egyptian, zero importers), and the wider MT-quality issues of the 868-exercise instruction corpus (typos like «اسحل/تقمص») which need a separate re-translation pass. Round 2 (caught by live production verification): the prefixed «الأكلة/أكلة» forms the word-boundary audit list missed — foods-explorer count labels + search placeholder/aria, the bottom-promo card (name/desc + the dialect CTA «شوف خطط الاشتراك»→«استعرض خطط الاشتراك»), the meal-planner page + AR metadata, the food-detail not-found/share strings («شارك الأكلة دي»→«شارك هذا الصنف الغذائي»), the CoachView client-invite hint, and the EVO chat local-reply generic fallback (was full Egyptian) — all unified to «صنف غذائي/مكتبة الأطعمة» MSA; manifest +4 files, vitest 1183/1183. Where: `src/lib/exercises.ts`, `src/lib/hub-collections.ts`, `src/lib/hub-depth-collections.ts`, `src/lib/hub-depth-equipment.ts`, `src/components/EvoFloatingWidget.tsx`, `src/lib/seo.ts`, `src/app/ar/foods/page.tsx`, `src/app/ar/foods/[slug]/page.tsx`, `src/components/views/PlansView.tsx`, `src/components/views/CoachView.tsx`, `src/components/views/CheckoutView.tsx`, `src/lib/ai-local.ts`, `src/lib/external-plan-text.ts`
- **Internal-pages MSA & terminology cleanup (Phase 204, owner order 2026-09-15 «تنظيف وتوحيد النصوص في الصفحات الداخلية فقط» — copy-only on public non-blog pages, zero pricing/limits/business-logic/API/DB/design changes, homepage + blog untouched):** (1) **Dialect removal (AR → natural Pan-Arab MSA):** the 8 ready program descriptions + their names/labels/alts (`workout-programs.ts` — «عايز/اللي/بس/البيت/تمرينة/الكور/تعلية» rewritten; the gym location label + descriptions + program names now read «النادي الرياضي», HIIT work/rest intervals «شغل/راحة»→«جهد/راحة»), the program-detail CTA («عايز خطة مخصصة ليك؟»→«تريد خطة مخصصة لك؟» + «بتعمل»→«تنشئ»), ALL five AR affiliate share templates + the affiliate-toolkit AR UI (were full Egyptian dialect — rewritten with identical facts/structure to the EN twins), for-coaches («للكوتشات/شغلك/فلوسك»→«للمدربين/عملك/أموالك» incl. AR metadata), StaticPageView («هل فيه كوتش بشري؟»→«هل يوجد مدرب بشري؟», «شات بوت»→«روبوت محادثة», «أكلات»→«الأطعمة», «وصول لخطط»→«وصولًا إلى خطط»), ContactView («سجل دخول»→«سجّل الدخول»), and the EVO comparison-table column («شات بوت عادي»→«روبوت محادثة عادي») + the EVO SoftwareApplication JSON-LD description (`seo.ts`). (2) **Old/contradictory claims fixed:** the «حجز/Book» framing of the Coaching SUBSCRIPTION retired from `faq-content.ts` (EN+AR — «you can book»→«subscription you can join»/«يمكنك حجزه»→«يمكنك الانضمام إليه», matching the already-fixed visible FAQ copy) and from both `/coaches/[slug]` fallback meta descriptions («Book private coaching»→«Coaching subscriptions with»/«احجز متابعة»→«اشترك في متابعة»); receipt-review attribution unified to «الفريق/the team» (memberships + StaticPageView FAQ — matches the admin-reviewed implementation, killing the «الكوتش» contradiction); the coaching data-safety FAQ answer aligned with the Phase-203-approved plain DB-level wording (no «مشفرة على Supabase مع RLS» jargon promise). (3) **Terminology/number unification with the system:** «كارب/كاربوس/كاربك»→«كربوهيدرات» across ALL tool surfaces + tools-shared hub data + foods-library labels/filters + the macro reference module (~45 occurrences — matches the homepage's Phase-203 term); «اعرف هل»→«اعرف إن كان» (BMI page + AR metadata + the shared hub source); «كوتش»→«المدرب» in marketing copy (EVO share title, memberships chip, receipt line, terms); «من هو EVO؟/Who is EVO?»→«ما هو EVO؟/What is EVO?» everywhere (matches the EVO page + AR FAQ); About's Premium tier now states the real number «4 توليدات خطط شهريًا/4 monthly plan generations» instead of the vague «خطط شهرية/monthly plans»; all AI-limit/Unified-Pool/tools-count copy verified to match `memberships.ts` verbatim (2/4/8/8 + 10 msgs/day + 8 tools — no drift found, nothing invented). (4) **Guards:** `marketing-msa-surface.test.ts` manifest extended to the program library + program detail + affiliate toolkit + static pages + FAQ/shared data sources (affiliate-content.ts itself is canary-only — its escapeHtml quote-regexes desync the naive tokenizer, documented in the test) + a Phase 204 describe pinning every removed phrase dead and the unified replacements present; `tool-msa-surface.test.ts` gained `tools-shared.ts` + a كارب/اعرف-هل canary. Where: `src/lib/workout-programs.ts`, `src/app/programs/**`, `src/app/programs/[slug]/ProgramDetailClient.tsx`, `src/lib/affiliate-content.ts`, `src/components/views/AffiliateToolkit.tsx`, `src/app/for-coaches/**`, `src/app/ar/for-coaches/**`, `src/components/views/StaticPageView.tsx`, `src/lib/faq-content.ts`, `src/app/coaching/page.tsx`, `src/app/memberships/page.tsx`, `src/app/evo/page.tsx`, `src/lib/seo.ts`, `src/lib/tools-shared.ts`, `src/app/tools/**` + `src/app/ar/tools/**` (BMI/macro/calorie), `src/app/meal-planner/page.tsx`, `src/app/ar/diet-plan/page.tsx`, `src/app/foods/[slug]/FoodDetailClient.tsx`, `src/app/ar/foods/[slug]/page.tsx`, `src/lib/foods-shared.ts`, `src/components/foods/FoodsFilters.tsx`, `src/lib/content/macro-calculator.ts`, `src/lib/comparisons.ts`, `src/lib/ai-local.ts` (one label), `src/app/coaches/[slug]/page.tsx` + AR mirror, `src/components/views/ContactView.tsx`
- **Homepage copy refinement (Phase 203, owner-approved 2026-09-15 — copy/micro-copy + link repositioning ONLY, zero design/data/quota/API changes):** (1) **Hero CTA → the account action:** guests (most homepage traffic) get «Create your free account / أنشئ حسابك المجاني» → `/auth?mode=signup` plus a quiet «Log in» link (auth has NO /ar mirror BY DESIGN — the bilingual auth page is the one destination from both locales), while signed-in members get «Go to your dashboard» (staff→`/admin` · coach→`/coach` · member→`/dashboard`, the header account-icon resolution law); the usage paths stay owned by the quick-nav chip (`#tools`) and the content sections — no duplicated CTA message; the hero subtitle is now concrete (free tools, real libraries, EVO + what a free account adds) while the H1, the quick-nav, and the dynamic seal counts are untouched. (2) **Training section:** «Browse by muscle group / تصفّح حسب المجموعة العضلية» + the seven live-count muscle chips + ONE clear `btn-outline` «All Exercises / كل التمارين» CTA now sit ABOVE the real samples; the old quiet duplicate bottom link is retired — sample data and fetching untouched (`getHomeSamples` props unchanged). (3) **Homepage FAQ reselection:** the five real pre-use questions (account needed? · AI generation free? · does my plan survive without an account? · what is EVO? · is my data safe?), every limit matching the implementation verbatim (guests 2 successful generations/month, success-only counting — `memberships.ts`/`tier-limits.ts`; guest-plan device persistence — `plan-persistence.ts`; database-level access control); the illogical «Does the site support Arabic?» question and the library-count question are retired (the seal chips + section copy answer them). (4) **Standalone `/faq`** (`faq-content.ts` JSON-LD + the visible `StaticPageView` copy): the Arabic-support question replaced by «What does a free account give me? / ماذا يمنحني الحساب المجاني؟» and the data-security answer rewritten in plain language with no promises beyond the implementation; everything else on the page untouched. (5) **Link bug fix:** both AI-generator pages' AR signup nudges linked `/ar/auth?mode=signup` — a route that does not exist (404) — and now link the real `/auth?mode=signup`. (6) **MSA micro-fixes:** كارب→كربوهيدرات (tools/nutrition section copy), «اعرف هل»→«اعرف إن كان» (BMI card), the «تصفّحها يبدأ من هنا / Start Browsing» heading suffix trimmed (EN/AR parity), «كن مدرباً»→«للمدربين» (header drawer + footer), tanween fixes (مجانًا/أولًا/تمامًا) and «حصل خطأ»→«حدث خطأ» in the newsletter form. Canaries: `homepage-adoption.test.ts` hero test re-pinned to the account-action contract + a Phase 203 describe (FAQ set + browse-above-samples order + retired copy stays dead); the `tool-msa-surface` security wording pin re-pinned. Where: `src/components/views/LandingView.tsx`, `src/components/SiteHeader.tsx`, `src/components/SiteFooter.tsx`, `src/components/NewsletterForm.tsx`, `src/app/ai-meal-planner/page.tsx`, `src/app/ai-workout-planner/page.tsx`, `src/lib/faq-content.ts`, `src/components/views/StaticPageView.tsx`, `src/app/faq/page.tsx`
- **Instant Signup — no email confirmation (restored 2026-09-08, migration 0074):** a new account is usable the SECOND the form submits: a DB-side auto-confirm trigger stamps `email_confirmed_at` at insert, and the client retries an instant password login when Supabase withholds the session — the user lands straight in the dashboard instead of a dead-end "check your email" screen (SMTP is unconfigured; see SECURITY.md for the trade-off and how to switch back)
- **GDPR Cookie Consent:** bilingual bottom banner (AR/EN) with the four cookie categories (necessary / preferences / analytics / advertising), an in-banner Privacy Policy link, equal-prominence Accept/Reject, a 365-day consent record, and one-click withdrawal via the "Cookie settings" button on the Privacy page — performance-safe by design: server-rendered with first paint, pre-paint hidden for returning visitors, compact fixed-position surface that never covers CTAs (reserved body padding)
- **Free Tools (the hub array in `src/lib/tools-shared.ts` is the count's single source):** calorie calculator, BMI, macros, body-fat %, water tracker, meal planner, and the two AI planners (meal + workout — free trial, no signup) — each with email delivery of results + lead capture where applicable, and inline bilingual validation feedback (no silent dead buttons)
- **Arabic Tool Mirrors (SEO-GEO-4, 2026-09-08):** every calculator plus the meal planner has a fully Arabic, server-rendered mirror under `/ar/tools/*` and `/ar/meal-planner` — Arabic metadata, canonical + reciprocal hreflang pairs with the EN pages, AR-aware cross-links (OtherTools / tools hub), and language-toggle mirror swaps — where: `src/app/ar/tools/*`, `src/app/ar/meal-planner/*`, paired metadata in the EN tool layouts
- **Comparison Hub (SEO-GEO-4, 2026-09-08):** `/compare` and `/ar/compare` index every "Alkemos vs competitor" page (ItemList + Breadcrumb schema, mirror links, sitemap-pages entry) — where: `src/app/compare/page.tsx`, `src/app/ar/compare/page.tsx`
- **Exercise Library:** 868 exercises with images (start + end positions), Arabic + English — fully bilingual data: nameAr/instructionsAr/tipsAr in MSA (imperative voice, Arabic-script fitness terminology per the 176 purity law — guards in `exercise-names-ar.test.ts` + `exercise-instructions-ar.test.ts`), feeding the AR pages, the Arabic HowTo JSON-LD, listing cards and the AI-planner prompt
- **Food Database:** 8,830 foods with per-100g macros
- **Workout Programs Library:** 7 structured programs (home, gym, HIIT…)
- **EVO AI Coach:** Floating chat widget with SSE streaming (live typing) — free users get 10 msgs/day; subscribers unlimited. EVO-5 layer: frequent-question cache (zero-client-policy `evo_chat_cache` — served ONLY to context-free requests, quota unchanged), per-dispatch telemetry (`evo_call_stats`) and the weekly AR/EN eval harness (GHA `evo-weekly-eval.yml` → `evo_eval_runs`, judge-scored quality curve) — see `/admin/evo-analytics`. Crisis shield (`evo-safety.ts`) answers self-harm/eating-disorder signals with a static safe redirect — they never reach a model; the plan-request intent gate (`evo-intent.ts`) routes plan phrasings (AR dialect families + EN) to the unified-pool gate for everyone and swap phrasings to the subscriber gate (Phase 183)
- **EVO Weekly Follow-up Email (EVO-3/D4, migration 0079 — live, P3-7 documented Phase 217):** an opt-in weekly check-in email from the AI coach — the member controls it from the opt-in card on `/profile` (strict opt-in per the owner's D4 decree: no silent enrollment, and cancellation is an update, never a row delete). Every send carries the member's REAL data only (weight delta from logged progress, active plan titles, next-step nudges — an empty log renders an honest "no measurements logged yet" nudge, never an invented number), sent from the send-only address `evo@alkemos.com` via Brevo's REST API on a minimum 7-day cadence per user (`last_sent_at` is stamped only on a successful send; one user's failure never aborts the batch). Dispatch runs through `POST /api/evo/followup/dispatch` (admin session OR the cron scheduler's `x-cron-secret` header — timing-safe; batch cap per run; the route answers 404 unless `EVO_FOLLOWUP_ENABLED` is exactly `true`) — where it lives: `src/lib/evo-followup.ts` (pure eligibility + builders, unit-tested), `src/app/api/evo/followup/dispatch/route.ts`, the `/profile` opt-in card, the `evo_followup_prefs` table — env flags: `EVO_FOLLOWUP_ENABLED`, optional `EVO_FOLLOWUP_FROM` (sender override), plus `EVO_CRON_SECRET` on the runtime
- **Coach Directory:** public coach pages (`/coaches/[slug]`) with landing editor, featured API, and preview mode
- **Blog:** AI-generated articles in Arabic + English (automated research→publish pipeline)
- **Save Results:** Free saves 3 results; premium tiers save 50–200 + PDF export
- **Meal Planner:** Custom meals with per-gram macro calculation (tier-scaled)
- **Progress Tracking:** Weight charts, body measurements, progress photos

### For Members (plan quotas — ONE unified monthly pool, Phase 183 «البوول الموحد» 2026-09-13)
- ONE monthly pool per identity for nutrition + workout AI generations COMBINED, counted on success only (failed attempts, input edits, navigation, and viewing an existing plan never burn quota)
- **Free (and guests, no signup):** 2 successful generations/month · 10 EVO messages/day
- **Premium ($14.99/mo or $119/yr):** Unlimited EVO chat · **4 generations/month (combined)** · 3 swaps/week · cross-device chat-history restore · 50 saved results
- **Pro ($29.99/mo or $239/yr):** **8 AI plan generations/month (combined)** · 6 swaps/week · 200 saved results · no ads (Phase 185: multiplier phrasing — "×2"/"doubled" — retired everywhere in favor of the direct numbers)
- **Coaching ($39.99/mo or $359/yr):** Human coach + every Pro limit (8 generations/month, 6 swaps/week) + weekly check-ins + direct coach contact
- The pool resets on the 1st (UTC). Previously generated plans stay visible after exhaustion. Editing and manual uploads are always unlimited.
- **Positioning ladder (owner repositioning, Phase 184):** Free = Experience → Premium = Manage → Pro = Adapt & Optimize → Coaching = Human Coach + AI. The homepage (free-AI-generation lead card), `/evo` (visitor/subscriber table rewritten against the ACTUAL enforcement — stale pre-unified-pool rows removed) and `/memberships` (tier taglines + the AI-quota vs save-limits separation note) present the same unified-pool numbers; the EVO widget shows the unified-pool meter to free users and guests too (lazy `?guestId` read of the same ledger). EVO memory (goals & preferences) stays FREE for every logged-in user (D1) — the PAID memory surface is the cross-device chat-history restore.
- **Unified AI-plan SAVE ladder (Phase 185, owner directive):** one three-step story on every surface (homepage lead card + FAQ, `/evo` comparison intro, `/memberships` trial note + FAQ): no account → the plan stays on that device (guest localStorage) · free account → saved permanently in the account & synced across devices (server auto-save to `plans`, plan-persistence.ts) · paid tiers → broader management (bigger save capacity + export) and extra benefits per tier. Premium's tagline/feature copy was corrected accordingly (permanent-save + sync is the free-account benefit; Premium sells the MANAGEMENT delta). The homepage tools grid no longer carries the AI Meal/Workout Planner cards — the planners are the flagship of the free experience via the lead card's «ولّد خطتك المجانية الآن» CTA; routes, Navbar, footer, /tools hub and all functionality are untouched. EVO-chat plan-save stays subscriber-gated in the widget (the comparison row is now explicitly scoped to it).

### For Coaches (B2B)
- **Client Dashboard:** filter tabs (active, expiring, no questionnaire, by tier…)
- **Client Management:** per-client tabs (overview, subscription, plans, AI plans, questionnaires, progress)
- **AI Plans:** generation draws from the CLIENT's own balance (ownership-checked); meal/item/day/exercise AI regeneration + manual edits are unlimited
- **Wallet System:** fixed per-client activation pricing paid to the site (one site-wide package list — single source `src/lib/coach-limits.ts`; the per-coach fee setting was retired 2026-09-18), receipt-reviewed top-ups (InstaPay/Vodafone Cash/PayPal), activation gate (402 insufficient_wallet)
- **Public Coach Page:** claimable slug + landing editor + ads + certificates
- **Affiliate Program:** 20% commission on referred subscription payments ($3.00 per Premium, $6.00 per Pro, $8.00 per Coaching), 7-day payout hold, reversal on refunds

### For Site Coaches (B2C — Phase 103)
- **Coach Kind:** every coach profile is `'site'` (platform-employed) or `'b2b'` (independent business) — one-tap toggle in `/admin/coaches`
- **Member Follow-ups:** admin assigns any member to a site coach via `/admin/site-assignments` (1↔1, move/unassign) — B2C members get human follow-up without touching the B2B money relation
- **Separation guarantee:** `site_coach_assignments` is deliberately a SEPARATE table from `coach_assignments` (the B2B money relation that drives wallet billing + affiliate attribution) — B2C rows can never pollute billing

### Platform & Admin (Admin Panel 2.0 — admin-only)
- **Dedicated AdminShell:** replaces AppLayout inside `/admin` — sectioned sidebar with active-state identity, live pending badges (payment requests, coach-page reviews), mobile tappable button grid
- **EVO Analytics `/admin/evo-analytics` (EVO-5):** dispatch volume + cache hit rate + avg latency + fallback share, provider success table, intent distribution, quota consumption (tamper-proof ledger), top cached questions, and the latest weekly eval run (avg score + per-question safety/language flags) — read-only, admin-gated route `/api/admin/evo-analytics` (service-role reads of the zero-client-policy 0081 tables)
- **Unified Client Roster `/admin/clients`:** every person in ONE page — type filter buttons (الكل/أعضاء الموقع/عملاء مدربي B2B/مدربو الموقع/مدربو B2B/الإدارة), membership lifecycle tabs (نشط/ينتهي قريباً/منتهي/بانتظار الدفع/بدون اشتراك), tier badges, test filter, search/sort/pagination; served by the role-aware `get_admin_clients_paged` RPC (0067 + 0068 correct classification: B2B client ⇔ assignment onto a REAL coach; admin follow-up shows «متابعة الإدارة»). Phase 143 (owner directive «الضغط فى اى مكان فى صف العميل يفتح ادارة العميل»): clicking ANYWHERE on a client row opens the deep manager `/coach/<id>` (the Phase-54 law restored on the admin surface after the Phase-103 unification) — the per-row «تعليم تجريبي» test-mark and «مسح» delete buttons were REMOVED by the same directive, while the checkbox column, «تحديد كل الظاهر» and the floating «مسح كل المحدد» bulk-delete bar REMAIN (143.1 owner amendment «امسح الازرار المحدده فقط واترك عامود مربعات التحديد ومسح كل المحدد»; same guarded `/api/admin/accounts` batch endpoint, checkboxes stopPropagation so selecting never triggers row navigation)
- **Finances `/admin/finances`:** SITE money (approved revenue, refunds, NET, pending + 6-month trend) separated from COACH money (prepaid wallet balances, top-ups, offline ledger + expected monthly bill)
- **Members `/admin/members`:** membership-status table (legacy redirect → unified roster)
- **Coaches Roster `/admin/coaches`:** the real coach list — kind badge, B2B clients + follow-up counts, membership status, wallet, one-tap site/B2B toggle
- **Site Assignments `/admin/site-assignments`:** pick site coach → search member → assign/move/unassign
- **AI Plans for Non-Members:** full generation + per-element regeneration (whole plan, one meal, one food item, one day, one exercise) + version history (cap 5) with reversible restore
- **Payments + 7-Day Refunds:** manual payment review, refund console with no-features-used enforcement, automatic affiliate commission reversal
- **Blog CMS:** AI pipeline with automated + manual modes
- **Coach System Center:** billing (fixed package pricing — single source coach-limits.ts), pages review, support, wallets, assignments, staff accounts

> **Note:** The tier priority in code is `pro` (3) > `premium` (2) >
> `free` (0). `coaching` is treated separately — it grants Premium-tier
> EVO access plus a human coach, but is NOT a higher membership tier.
> There is no `elite` tier in the actual implementation.

---

## 🔧 Tech Stack

| Layer | Technology |
|---|---|
| **Framework** | Next.js 16 (App Router) |
| **UI** | React 19, Tailwind CSS 4, shadcn/ui (new-york style) |
| **Backend** | Supabase (Postgres, Auth, Storage, RLS + GitHub-integration auto-migrations) |
| **AI** | OpenRouter + Groq + NVIDIA NIM ONLY — unified layer (`src/lib/ai-provider.ts`), 3-way provider-lead rotation + interleaved strongest-chain + Promise.any race + SSE streaming, budget-clamped ≤52s |
| **Charts** | Recharts 3 (lazy-loaded) |
| **Forms** | react-hook-form + zod |
| **Email** | Brevo REST API (tool results over HTTPS, daily 100/24h code cap) + Supabase custom SMTP (auth emails via Brevo relay) |
| **Payments** | PayPal (create/capture/webhook) + manual review (InstaPay/Vodafone Cash/bank) |
| **Analytics** | Vercel Analytics + Speed Insights + GA4 (optional) |
| **Ads** | Google AdSense (tier-gated, auto-suppressed on auth routes) |
| **PWA** | manifest.json + service worker |
| **Deployment** | Vercel (Singapore region) |
| **Type System** | TypeScript 5, `strict: true`, 0 `@ts-nocheck`, 0 `ignoreBuildErrors` |

---

## 📊 Database

The database evolves through a versioned migration registry in four
naming families (see
[`supabase/migrations/INDEX.md`](./supabase/migrations/INDEX.md) — the
single documented home of the numbering range). All auto-applied families carry
Row Level Security (RLS) policies — hardened progressively (Phases
99–100: strict RLS for `progress_photos` and the tamper-proof
`plan_swaps` usage ledger, with table-level revokes for loud failures).
Key tables:

| Table | Purpose |
|---|---|
| `profiles`, `subscriptions`, `subscription_requests` | Accounts, active tiers, manual payment requests; `profiles.coach_kind` = `'site'`\|`'b2b'` (0067) |
| `plans`, `plan_swaps`, `meal_plans` | Member plans + weekly swap tracking (immutable usage ledger) |
| `nutrition_questionnaires`, `fitness_questionnaires`, `progress_entries`, `progress_photos` | Intake + tracking |
| `chat_messages`, `evo_chat_usage` | EVO chat history + daily usage ledger |
| `ai_jobs` | Queued AI jobs (chat/plan/swap/regeneration) with staff gates |
| `blog_posts`, `blog_generation_queue` | Bilingual blog + pipeline state |
| `saved_results`, `tool_leads` | Saved tool results (customers DB) + leads from every tool + signups (0059/0060: name/type/newsletter) |
| `referrals`, `referral_earnings`, `referral_payouts` | Affiliate tracking, 20% commissions (7-day `available_at` hold), payouts |
| `coach_assignments`, `coach_payments` | B2B MONEY relation (wallet billing + affiliate attribution) + activation ledger |
| `site_coach_assignments` | B2C member↔site-coach 1↔1 follow-ups (0067 — deliberately separate from money) |
| `coach_wallets`, `coach_topup_requests`, `coach_wallet_transactions` | Coach wallet + audited top-ups |
| `coach_emails`, `support_tickets`, `ticket_messages`, `notifications`, `admin_notifications` | Comms |
| `external_plans` | Admin-generated AI plans for non-members (RLS: admin only) |
| `refund_requests` | 7-day refund flow (RLS: admin + owner read) |
| `get_admin_clients_paged` / `get_admin_clients_stats` | Role-aware roster RPCs (0067, corrected 0068) covering every role — members, B2B coaches, site coaches, staff |

> The three Phase-5 ad-hoc tables (`plan_swaps`, `progress_photos`,
> `coach_presence`) are now IN the migration registry: back-filled by
> `0063`, RLS-hardened by `0064`/`0065` (closed Phase 99-run, 2026-09-03).

### Backups (free-tier replacement for Supabase paid backups)

The Supabase free plan ships **no scheduled backups** — so a dedicated
GitHub workflow (`.github/workflows/db-backup.yml`) takes over:

- **What**: a full JSON snapshot of every public table + `auth.users`
  export, written by `scripts/db-backup.mjs` (PostgREST pagination,
  deterministic ordering so git diffs show only real changes).
- **Where**: the **private** repo `muscleshubfit-cpu/alkemos-backups`
  (snapshots contain user PII — they must never land in this public repo
  or in public artifacts). Layout: `snapshots/<date>/tables/<table>.json`
  + `manifest.json` + `auth_users.json`.
- **When**: daily after the morning article lands, plus a manual
  **Run workflow** button (Actions → DB backup). Each day is a commit →
  point-in-time recovery (a row deleted today is in yesterday's snapshot).
- **Restore**: `scripts/db-restore.mjs` — dry-run by default, `--apply`
  upserts rows back (`on_conflict=id` merge, nothing deleted). Passwords
  are not exportable — restored users use "forgot password".
- **Not covered**: Supabase Storage files (progress photos etc.) —
  they live outside the database.
- **Secrets used**: `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`
  (existing) and `BACKUP_REPO_TOKEN` (push access to the private repo).

---

## 🌍 Languages

- **English** (primary) — `/`
- **Arabic** (RTL) — full mirror under `/ar/*` (blog, exercises, foods, programs, coaches, for-coaches, memberships, faq…)
- Language toggle in header (custom i18n provider — no `next-intl`)
- `Content-Language` header set via middleware
- **Dynamic `lang`/`dir` on `<html>`** — route-locale aware (Arabic routes render `lang="ar" dir="rtl"` at the root tag)
- hreflang alternates on blog articles

---

## ⚡ Performance

Performance optimizations were applied during Phase 6 (2026-08-19) and the
project has been running in production since. For current performance
metrics and any ongoing optimizations, see `worklog.md` recent entries.

- AI chat uses an interleaved strongest-models chain (OpenRouter ↔ Groq, server-side usage ledger) with **SSE streaming** — raw tokens arrive live (`event: delta` → `event: final`)
- Every sequential AI path is budget-clamped to `maxModels × timeoutMs ≤ 52s` (Vercel Hobby 60s cap)
- Long-running generations (blog pipeline, AI jobs) run in GitHub Actions runners with retries instead of serverless timeouts
- Image optimization: AVIF + WebP formats enabled
- Static asset caching via service worker (PWA installable)

| Feature | Before | After | Improvement |
|---|---|---|---|
| **EVO AI Chat** | 18-25s + thinking artifacts | 1.4-3.9s + clean streaming responses | **5-7x faster** ✅ |
| **Plan generation** | 90-180s timeout | 30-60s (or GHA queue) | **3x faster** ✅ |
| **Swap (meal + exercise)** | 60-90s | 10-30s | **3-4x faster** ✅ |
| **Article generation** | Timeout + short articles | ~100s + 600-900 EN words + 500-800 AR words | **No more timeouts** ✅ |

### AI Provider Strategy

One unified layer (see [`DEVELOPER_GUIDE.md`](./DEVELOPER_GUIDE.md) §14):

| Function | Use case | Behavior |
|---|---|---|
| `callAI()` | Single entrypoint | Routes to OpenRouter/Groq with model chain |
| `callAIWithFallback()` | Plans, Articles, Research | Sequential strongest-chain with fallbacks |
| `callFreeOpenRouterRace()` | Swap | Parallel — races top models via `Promise.any()`, returns first success |
| `callFreeAIFallbackChain()` | EVO chat, Blog pipeline, Plans | Sequential interleaved chain (fast/strongest) + key rotation — optional `onDelta` forwards raw tokens live |
| `callAIStream()` | EVO chat (Phase 89) | Streaming single-provider call — `stream:true`, forwards SSE content deltas to `onDelta`, returns full text; reasoning deltas buffered silently |

**EVO chat streaming (Phase 89):** the chat endpoint answers with SSE —
`event: delta` (raw tokens, live typing) → `event: final` (cleaned full
text + links + source) — while 429/errors stay JSON.

**Owner directive (2026-08-27):** the platform uses **OpenRouter and Groq
ONLY** through `src/lib/ai-provider.ts`. The old direct Gemini SDK /
OpenAI / Anthropic / DeepSeek integrations were removed; Google models
are reachable via their OpenRouter slugs (`google/*`). The GitHub
Actions workflows add an outer retry loop (3 attempts, 120s backoff)
around each pipeline step.

---

## 🔍 SEO & GEO (تحسين محركات البحث والظهور للذكاء الاصطناعي)

الاستراتيجية الحالية (Phase 117 — تدقيق وتحسين مبنى على أفضل ممارسات 2026):

- **الإجابة الذرية:** الصفحة الرئيسية تفتح بفقرة مباشرة واحدة قوية أسفل العنوان الرئيسي (H1) تلخص المنصة كاملة — كلمات مفتاحية ثانوية (منصة التدريب الرقمي، خطط مخصصة، مدربون معتمدون) + الأرقام الحية (عدد التمارين والأغذية مباشرة من الكود) — مصممة لاقتناص الـ featured snippets وإجابات نماذج الذكاء الاصطناعي (دُمج فيها قسم «ما هي Alkemos؟» السابق بالكامل لإزالة التكرار — تصحيح Phase 117).
- **جداول المقارنة:** صفحات المقارنة التخصصية (`/compare` ومراياها) تحمل جداول مقارنة سريعة القراءة (✅/❌) بين المنصة والبدائل (MyFitnessPal · Freeletics · ExRx) بمصدر واحد `src/lib/comparisons.ts` — يُظهر القيمة المضافة بوضوح. (المرحلة 202: جدول الرئيسية السريع نُزل مع قسم العضويات من الرئيسية بترتيب المالك «لا تعرض Membership pricing أو جداول الأسعار في Homepage» — محتوى الجداول التخصصية كامل بلا مساس.)
- **البيانات المنظمة (JSON-LD):** `Organization` على مستوى الموقع كله بحقول المالك (name · url · logo · description مختصرة من الإجابة الذرية · sameAs) بدلاً من LocalBusiness بقرار المالك 2026-09-04 — لا نشاط محلي والمشروع عالمي، وبلا أي حقول عنوان/هاتف/منطقة (sameAs يحمل رابط الموقع فقط: لا بروفايلات تواصل مملوكة موجودة) · `WebSite` (مع SearchAction) · `FAQPage` في الرئيسية (أسئلة المالك الحرفية) · `Article` في صفحات المدونة · `Service` / `SoftwareApplication` / `BreadcrumbList` / `ItemList` في الصفحات المتخصصة — كلها مركزية في `src/lib/seo.ts`.
- **ملفات الزحف:** `robots.txt` يسمح لكل الزواحف العامة **وزواحف الذكاء الاصطناعي** (GPTBot · ClaudeBot · PerplexityBot · OAI-SearchBot…) ويحجب صفحات الإدارة والحسابات (`/admin`, `/dashboard`, `/api/`) · `sitemap.xml` ديناميكي (App Router) يغطي كل الصفحات العامة بمرايا عربية و`hreflang` متبادل (بإشراف المالك: صفحات المدربين التفصيلية خارج السايت ماب — تصحيح Phase 117).
- **ميتا وصف قصير بدعوة + عناوين H2 تسويقية:** الميتا description بين 150-160 حرفًا بدعوة واضحة (CTA) في اللغتين (EN «Start free today!» · AR بحرفية دعوة المالك «ابدأ رحلتك الرياضية الآن...») · عناوين H2 في الرئيسية بصيغة تسويقية جذابة ثنائية اللغة (تصحيح بأمر المشرف 2026-09-04 بعد ملاحظة المالك أن صيغة الأسئلة المنتظمة كانت مملة ومصنوعة) — تغطية الاستعلامات السؤالية باقية عبر أسئلة FAQPage JSON-LD الحرفية.
- **GEO (تحسين الظهور للذكاء الاصطناعي):** `llms.txt` (دليل آلة مختصر + تعليمات إجابة صريحة: أسئلة اللياقة والتغذية فقط، بلا نصائح طبية) · `llms-full.txt` (الدليل الموسع + أحدث المقالات لكل لغة) · خلاصات RSS ثنائية اللغة.
- **كاش الصفحات العامة (SEO-GEO-4، 2026-09-08):** سياسة الكاش `public, max-age=0, must-revalidate, s-maxage=3600, stale-while-revalidate=86400` معرّفة في `next.config.ts` headers() للمسارات العامة فقط (الخاصة/المصادقة/API مستثناة segment-exact وتبقى no-store) + كوكي اللغة يُكتب عند التغيّر فقط في `src/middleware.ts` — **حقيقتان متحققتان حيًا:** ① على التشغيل الذاتي next.config headers() يتغلب على رأس إطار العمل ② **على Vercel الإنتاجية يُجبَر `private, no-store` على كل صفحة ديناميكية مهما فعلت الرؤوس** — لذا التفعيل الإنتاجي يتم عبر Cloudflare Cache Rule (الدليل المبسط في `docs/SEO-GEO-MASTER-PLAN.md` §12 SEO-GEO-4).
- **هوية العلامة العربية (SEO-GEO-4):** كل عنوان عربي ينتهي بعلامة واحدة (إصلاح تكرار «Alkemos — Alkemos») عبر `stripTrailingBrandForArTemplate()` في `src/lib/seo.ts` + صفحات الأدوات والمقارنات العربية بميتا عربية مستهدفة.
- **جودة محتوى المدونة (المرحلة 178، 2026-09-12):** كل مقال يعرض قسم الأسئلة الشائعة **مرة واحدة** — `stripFaqSectionFromBody` في `src/lib/blog-msa.ts` يقص قسم FAQ من متن الماركداون عند الرسم متى وُجدت بطاقات `faq_json` (قانون «عرض واحد» §12.22 مطبق على المقالات القديمة أيضًا) · الوصف والمقتطف عند النشر يقصّان عند حد الكلمة بعلامة ختام مضمونة (`clampMetaDescription` في `src/lib/blog-pipeline.ts`) · 5 تحويلات 301 في `next.config.ts` توحد مقالات AR كانت متآكلة كلماتيًا (عائلة النوم + زوج المبتدئين — ميجريشن 0084) · فشل جلب مقال عند العرض = 404 صادقة لا قشرة فارغة 200 (حادثة الكاش المسموم).
- **خريطة النوايا وبوابة المواضيع (المرحلة 192، 2026-09-14 — تدقيق SEO/GEO):** `src/lib/intent-map.ts` هو السجل الوحيد للنوايا البحثية الأساسية (20 عنقودًا: Query Cluster → Primary Intent → Canonical Page → Supporting Pages) — EVO هو الصفحة الأساسية الوحيدة لنية «AI fitness coach»، ولكل حاسبة/مركز/مكتبة canonical واحد لا يتنافس عليه. **البوابة المستقبلية:** `findBlogIntentCollision` موصولة بمنتقي مواضيع المدونة (`pickSmartTopic` + `getFallbackTopic` في `src/lib/blog-topics.ts`) فأي موضوع جديد يطابق استعلامًا رأسيًا لنية أساسية (بعد تطبيع عربي: همزات/تاء مربوطة/تشكيل) يُرفض آليًا — مقالات المدونة تربط بالصفحات الأساسية ولا تتنافس معها. **تصنيف الكوربوس (69 مقالًا) بتدقيق زحف حي كامل:** دمج+301 لمكررَي نية (`muscle-building-bodyweight-home` → `home-muscle-building-guide-no-equipment` و`4-week-beginner-hypertrophy-plan` → `4-week-beginner-muscle-building-plan` — ميجريشن 0087 + 301s في next.config، بلا حذف محتوى) + إعادة توجيه عنوان `calculate-calories-fat-loss` إلى نيته الحقيقية (برنامج 8 أسابيع) + قصّ FAQ المكرر من 11 مقالًا legacy (عقد عنوان كامل + حلقة قص في `stripFaqSectionFromBody`) + إصلاح 3 روابط داخلية مكسورة (0087-C) + شبكة EVO ثنائية الاتجاه (EVO↔المولدين↔الكوتشينج) + 4 قواعد ربط تحريري جديدة (AI planners/EVO/Coaching) في `blog-tool-links.ts` — التفصيل الكامل والتصنيف في `docs/SEO-GEO-MASTER-PLAN.md` §12.49.
- **موافقة الكوكيز وGA (المرحلة 178):** Consent Mode v2 يُضبط **default-denied قبل** `gtag('config')` في `src/app/layout.tsx` ثم يُطبَّق اختيار الزائر المخزّن قبله — لا تتبع GA/AdSense قبل الموافقة (التفصيل في `SECURITY.md` §7).
- **فصحى السطح التسويقي (المرحلة 178):** صفحات `/ar/evo` والرئيسية `/ar` والعضويات والكوتشينج بالفصحى الحديثة السهلة Pan-Arab — نفس قانون المدونة 175/176 (حراس `marketing-msa-surface.test.ts`).

> المصادر الحية للتقنيات دي في الكود: `src/lib/seo.ts` · `src/app/sitemap.ts` · `public/robots.txt` · `public/llms.txt` (وفق §12.8 — الكود هو مصدر الحقيقة).

---

## 🐛 المشكلات المفتوحة

قائمة محدثة بالمشكلات المعلقة متوفرة في [`archive/`](./archive) وسجل التغييرات.  
حالياً، لا توجد مشكلات حرجة مفتوحة؛ معظم المشكلات السابقة قد حُلّت في المراحل السابقة (راجع الأرشيف للتفاصيل).

---

## 📌 الحالة الراهنة

لمتابعة أحدث تطورات المشروع والمرحلة الحالية والمهام المفتوحة، يرجى الرجوع إلى الملف الرسمي: [`STATE.md`](./STATE.md).

---

## 🤝 المساهمة

نرحب بالتقارير والاقتراحات عبر [Issues](https://github.com/muscleshubfit-cpu/alkemos/issues).  
للمزيد من التفاصيل حول سياسة المساهمة (وهي خاصة)، راجع [`CONTRIBUTING.md`](./CONTRIBUTING.md).

---

## 📝 License

This project is **proprietary**. All rights reserved.

The repository is public for development, auditing, transparency, and
agent-assisted collaboration — but the code is **NOT open source**.

See [`LICENSE`](./LICENSE) for the full proprietary terms.

---

## 📋 Additional Documentation

- [`docs/README.md`](./docs/README.md) — **Documentation index & lifecycle registry** (what is LIVE / FROZEN / HISTORICAL + the per-session reading budget)
- [`STATE.md`](./STATE.md) — **الحالة الرسمية الحالية** (أول ملف يُقرأ في أي جلسة — AGENTS.md §3.6)
- [`AGENTS.md`](./AGENTS.md) — AI agent operating rules (required reading)
- [`SECURITY.md`](./SECURITY.md) — Security policy
- [`archive/PROGRESS.md`](./archive/PROGRESS.md) — Status history FROZEN at Phase 115 (merged into `STATE.md`)
- [`DEVELOPER_GUIDE.md`](./DEVELOPER_GUIDE.md) — Developer onboarding + architecture details
- [`archive/QA_CHECKLIST.md`](./archive/QA_CHECKLIST.md) — Verification evidence FROZEN at Phase 115 (merged into `STATE.md`)
- [`supabase/migrations/INDEX.md`](./supabase/migrations/INDEX.md) — Binding migration registry + naming laws
- [`docs/`](./docs/) — SEO frameworks (CWV thresholds, E-E-A-T, schema reference) + historical audits
- [`.env.example`](./.env.example) — Environment variables reference
- [`worklog.md`](./worklog.md) — Per-agent change log — active window (top-12 entries + rolling date buffer) live; the full append-only history continues in [`archive/WORKLOG_ARCHIVE.md`](./archive/WORKLOG_ARCHIVE.md) (pre-2026-09-10 tail rotated there verbatim — Phase 237)
