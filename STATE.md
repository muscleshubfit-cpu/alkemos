# STATE.md — الحالة الرسمية للمشروع (أول ملف يُفتح في أي جلسة)

> **قانون (AGENTS.md §3.6):** ده أول ملف أي وكيل يقرأه قبل أي شغل — وبيتحدث إلزاميًا في نفس الفريم اللي بيغيّر الحالة.
> الملف محدود بـ 100 سطر بوابةً (`scripts/docs_audit.py`) — اكتب مضغوط.
> **قانون التوحيد (Phase 115):** الملف ده هو **المصدر الرسمي والوحيد** لحالة المشروع الحية — `PROGRESS.md` و`QA_CHECKLIST.md` مجمدون في `archive/`.
> **آخر تحديث:** 2026-09-11 (المرحلة 168 — EVO-5: كاش الأسئلة الشائعة + مقياس الجودة الأسبوعي + تصدير المحادثة + لوحة تحليلات الأدمن — W5 المتبقي كامل بأمر المالك «ابدأ evo 5») فوق 167 (EVO-4: التعلم من الخطط W4) فوق 166 فوق 165 فوق 164 فوق 163

## المرحلة الحالية
- **المرحلة:** 168 (EVO-5: W5 المتبقي كامل — كاش + eval harness + تصدير + لوحة تحليلات) فوق 167 (EVO-4: التعلم من الخطط W4 كامل E1+E2+E3) فوق 166 (EVO-3) فوق 165 (EVO-2) فوق 164 (EVO-1) فوق 163:
- **(أ) 168 — EVO-5 منفذ (W5 المتبقي كامل بأمر المالك «ابدأ evo 5»):** **ميجريشن 0081** — ثلاثة جداول ملاحظة منصة RLS مفعّل **صفر سياسات عميل** (posture 0080 موثق بINDEX.md): `evo_chat_cache` (كاش الأسئلة الشائعة: question_hash UNIQUE=sha256 للتطبيع · question_norm لtrigram · language ar/en · answer · source · hits · expires_at TTL 48س + إضافة pg_trgm + دالة `evo_cache_lookup` بضربة واحدة: hash أولًا ثم أعلى تشابه بنفس اللغة غير المنتهي) · `evo_call_stats` (صف لكل نداء نموذج حقيقي: provider openrouter/groq/nvidia/cache/local · intent general/plan_nutrition/plan_workout/swap · cache_hit · success · latency_ms · user_id nullable) · `evo_eval_runs` (نتائج التقييم الأسبوعي: question_id · score 0-10 · safety_pass · language_match · notes · answer_excerpt 400 حرف بلا PII مستخدم) — **الكاش (fail-open):** بوابة نقية `isCacheEligibleMessage` في `evo-cache.ts` (تاريخ فارغ · بلا سياق مشترك · بلا ذاكرة · بلا نية خطة/استبدال · بلا مقابلة أول لقاء · ≥12 حرفًا مطبعة) — الرسالة المخدومة من الكاش **تستهلك حصتها كالعادة** (record-before-dispatch قبل البحث) والروابط تُحسب حية للسؤال الجديد — الكاش يخدم كرد SSE مقطع (chunkForSse) فيتصرف كبث حي — لا embeddings ولا مزود رابع («دلالي» = تطبيع نصي + تشابه trigram 0.92) — **برومبت النظام مصدر واحد:** استخراج حرفي `buildSystemPrompt` + EvoClientContext من مسار الشات إلى `evo-system-prompt.ts` (المسار والتقييم يقرآن نفس البرومبت — لا forks) — **eval harness:** `evo-eval.ts` نقي (10 أسئلة مرجعية 6AR+4EN بفئات general/nutrition/exercise/food-calories/safety-boundary/subscriber-gate/tools · باني برومبت القاضي بJSON صارم · parseEvalVerdict دفاعي — فشل القاضي = خطأ ناعم لا درجة مختلقة · summarizeEvalResults) + `evo-eval-runner.ts` (server — يجيب بالبرومبت الحقيقي baseline مجهول عبر السلسلة tag evo-eval + قاضي رخيص tag evo-eval-judge + تنظيف بنفس مسقمات المسار + insert لكل سؤال — صفر درجات = تشغيل أحمر) + سكربت `scripts/ai-jobs-runner/evo-eval.mts` (exit 2/1/0 صادق) + workflow **evo-weekly-eval.yml** (خميس 21:00 UTC = جمعة 00:00 القاهرة + workflow_dispatch — يحتاج مفاتيح AI مثل process-ai-jobs) — **تصدير المحادثة (W5.6):** `evo-export.ts` نقي (نسخ = markdown نصي للكليبورد · PDF = نافذة طباعة منسقة RTL/LTR بلا أي اعتمادية جديدة — حوار الطباعة يوفر «حفظ PDF» على الهاتف) — زران برأس الويدجت (Copy/FileDown) يظهران بوجود رسائل وأثناء عدم البث — يخطيان فقاعات الحصص/الأخطاء (⏰/عذراً) مثل تقييم 👍/👎 — قراءة فقط: بلا كتابة تاريخ وبلا نداء API — **لوحة تحليلات الأدمن (W5.8):** `/api/admin/evo-analytics` (requireAdmin + service-role — تجميع JS لآخر 30 يوم: نداءات/نسبة كاش/زمن متوسط/مزودون بنجاح ونطاق/نوايا/حصص من سجل evo_chat_usage المضاد للعبث/👍👎 من evo_feedback/أعلى أسئلة الكاش/آخر دورة تقييم بنافذة 10 دقائق) + `/admin/evo-analytics` (AdminEvoAnalyticsView: 4 كروت KPI + 7 أقسام) + إدخال AdminShell «تحليلات EVO» — **النسخ:** بطاقة «صدّر محادثتك بلمسة» + صف differences بـ/evo — **+31 اختبارًا: 513/513** — INDEX.md 0081 + مرآة types.ts (3 جداول + الدالة) بنفس الكوميت
- **(ب) 167 — EVO-4 منفذ (W4 E1+E2+E3، قرار المالك §7.3 «نعم يتعلم من كل شيء»):** **ميجريشن 0080 `evo_nutrition_patterns`** (bucket مقيد: calories_by_goal/macros_by_goal/meal_count/food_frequency/exemplar/swap_volume/swap_removed/swap_added · UNIQUE(bucket,key) · RLS مفعّل **صفر سياسات عميل** عمدًا — posture evo_memory_state: معرفة منصة مجمعة بلا صفوف ملكية، service-role الكاتب/القارئ الوحيد — انحراف موثق بقانون RLS في INDEX.md) — **E1 تجميع نقي بلا LLM (صفر تكلفة):** `evo-nutrition-learning.ts` نقي client-safe (classifyGoal بنفس عائلات regex المولد · normalizeFoodName عائلة تطبيع evo-memory مع شطف «ال» · extractMealItems/extractNutritionNumbers يفهمان **شكلَي الخطط الحقيقيين** plans content + meal_plans plan_data · aggregateNutritionPatterns: توزيعات كالوري/ماكرو حسب الهدف (حارس هدف-مجهول لا يلوث maintain) · meal_count · food_frequency بdedupe لكل خطة) — **أرضية صدق:** أي bucket تحت الحد (3 خطط هدف / 3 خطط صنف / 3 استبدالات) **يُحجب** لا يُعرض كمعرفة — **E2 exemplar:** هيكل مرجعي مجهول ببناء حتمي من حقول مهيكلة فقط (overview/notes/عناوين لا تدخل أبدًا فلا تسرب أسماء بالبناء) + pickExemplarSource (المعتمد أولًا ثم اكتمال الهيكل ثم الحداثة) — **E3 تعلم الاستبدال:** الحجم من plan_swaps (نوع فقط — حد موثق) + **هوية الأصناف الحقيقية** من diff old/new محتوى member-edit swap (best-effort لا يعطل الاستبدال أبدًا، دمج read-modify-write service-role) — **المشغّل الأسبوعي:** `evo-learning-runner.ts` (E1 delete-family+insert كلي · exemplar/swap_volume upsert · swap_* لا يُمس) + سكربت `scripts/ai-jobs-runner/evo-learning.mts` + workflow **evo-weekly-learning.yml** (أحد 21:00 UTC = إثنين 00:00 القاهرة + workflow_dispatch — حتمي فلا يحتاج مفاتيح AI، سكريتا Supabase فقط) — **حقن المولد:** `evo-nutrition-knowledge.server.ts` fail-open ("" عند أي فشل/فراغ — التوليد لا يعتمد على التعلم) → generateNutritionPlanAI بارامتر ثالث + regenerateMeal بارامتر سادس (block بعد buildVarietyBlock / بعد avoidBlock) عبر كل المتصلين: ai-job-processors (plan_nutrition + meal_regenerate) و admin/external-plans (توليد + regenerate_meal) — **+33 اختبارًا: 482/482** — INDEX.md 0080 + مرآة types.ts
- **(ج) 166 — EVO-3 منفذ (سلوك المدرب الحقيقي W2 + بنية D4):** **W2.1 استيعاب أول لقاء:** بوابة نقيّة `needsFirstMeetingInterview` في `evo-coach.ts` (مشترك + نية خطة + بلا استبيان مطابق بالمجال) → حقن FIRST-MEETING PROTOCOL في buildSystemPrompt (حتى 4 أسئلة سؤالًا لكل رد → ملف مجمّع + افتراضات مصرّح بها → توليد في نفس الرد) — **W2.2 قراءة التقدم الحقيقي:** إصلاح **انجراف صامت** (recent_measurements تُحمّل من DB منذ قَبل ولا تُسلسل أبدًا!) — `formatProgressForPrompt` يحقن آخر قياسات الوزن/الوسط + دلتا «التغير عبر الفترة» (stable < 0.2كغ) وغياب القياس يدعو للتسجيل بلا اختراع أرقام — **W2.4 سقف السياق:** 10→16 للمشتركين (resolveHistoryCap — الكلام يُقص عند 16 ثم تقطيع حسب المستوى، الأحرار والمجهول 10 كما هي) — **W2.3 بنية المتابعة D4 (بنية بلا تفعيل):** ميجريشن `20260910200000_0079_evo_followup_prefs.sql` (opt-in إلزامي default false · RLS: select مالك/أدمن · insert مصادق بصفوفه · update مالك/أدمن · **delete أدمن فقط** · فهرس جزئي where opted_in) + أنواع types.ts (1:1 FK profiles) + باني بريد نقي `evo-followup.ts` (isFollowupDue بإيقاع 7 أيام · أرقام حقيقية فقط · esc محارف HTML · تذييل opt-out بالمسار الحقيقي /profile · عربي/إنجليزي) + `/api/evo/followup/dispatch` (بوابة 1: EVO_FOLLOWUP_ENABLED=true وإلا 404 · بوابة 2: requireAdmin أو x-cron-secret · دفعات ≤50 · ختم last_sent_at على النجاح فقط · مُرسل evo@alkemos.com إرسال-فقط بلا replyTo) — **ترتيب التفعيل الموثق:** فحص SMTP/Brevo حي بمُرسل evo@alkemos.com → علم البيئة + EVO_CRON_SECRET → واجهة opt-in (لا واجهة قبل التفعيل — لا وعد بلا تنفيذ) — **+35 اختبارًا: 442/442** (21 evo-coach + 14 evo-followup) — INDEX.md 0079
- **(د) 165 — EVO-2 (الذاكرة الدائمة W3):** ميجريشن 0078 (`evo_memory` RLS نمط chat_owner_or_coach — **delete أدمن فقط D2** · `evo_memory_state` عداد مضاد للعبث **صفر سياسات عميل**) — استخلاص كل 10 رسائل عبر `after()` (صفر لاتنسي) بسلسلة fast وقائمة رفض PII — حقن أعلى 15 حقيقة **لكل المسجلين (D1 مجانية)** — /evo بقانون صدق النسخ — **+22: 407/407** — INDEX.md 0078
- **(هـ) 164 — EVO-1 (دروع YMYL + شخصية المدرب + 👍/👎):** `evo-safety.ts` نقي (detectEvoCrisis بدقة عالية — «التمرين ده قتلني» لا يشعل) قبل الحصص (الأزمة لا تُحسب) — buildSystemPrompt بمواقف المدرب — 0077 evo_feedback append-only + /api/ai/feedback (service-role · 20/د/IP · فشل=200 saved:false) + أزرار 👍/👎 بالويدجت — **+18: 385/385** — INDEX.md 0077
- **(و) 163 — خطة تطوير Evo:** docs/EVO-MASTER-PLAN.md (W1-W6 · EVO-1..6) — قرارات المالك الخمسة موثقة §7 (ذاكرة مجانية · لا انسَ-كل-شيء · يتعلم من كل شيء مجهول الهوية · D4 معتمد ببريد evo@alkemos.com إرسال فقط · W6 بعد EVO-4)
- **(ز) 162 — توحيد مسار الكوتش:** مسار الكوتش (article_generate) صار **نفس مسار التوليد الآلي**: رابط الـenqueue يُرسل `blog-post-{ar|en}.yml` عبر workflow_dispatch (`blog-pipeline-dispatch.ts` بنفس `GITHUB_DISPATCH_TOKEN`، fail-open) فلا يرث المقال خط P0→P5 كاملًا (بحث ← محتوى 1500-2500 كلمة ← صور ← مراجعة ← نشر + مصافحة الاقتران linked_post_id) — صف ai_jobs يصير **إيصال إرسال** (done + pipelineDispatched) والطابور الحقيقي blog_generation_queue — موضوع الكوتش (≥10 أحرف قابلة للاختام) يمر inputs → `PIPELINE_TOPIC` → P0 فيُختَم في جهتي من الـbrief (`withCoachTopic` نقي +4 اختبارات) والتوأم يحتفظ بموضوعه المبحوث — جدولة الأتوماتيكي ومجموعات التزامن وحصة مقال/يوم/لغة **لم تُمس** (الإرسال يضيف تشغيلًا للـworkflow نفسه فقط) — المولد الأحادي مسودة يبقى **الاحتياط** عند فشل الإرسال — الموضوع القصير (<10) = البحث يختار (نفس سلوك الأتوماتيكي) — الأزواج المتَبنّى/المنضمّة تتجاهل التجاوز — +15 اختبارًا (367/367)
- **(ح) 161 — ثالث مزود NVIDIA:** `NVIDIA_API_KEY` انضم لسلسلة الاحتياط — تدوير الرائد ثلاثي (openrouter→groq→nvidia؛ fast يبقى groq-first) — provenance بالاسم الفعلي للمزود — استكمال 161.1-161.5 في قسم QA أسفل
- **(ط) 160 — إصلاح CI:** ffbfa15 حذف @vercel/og دون كوميت bun.lock → `--frozen-lockfile` فشل في كل workflow يثبت اعتماديات (7 process-ai-jobs × 5 quality-gate منذ 2026-09-08 17:30 UTC — طابور ai_jobs عالق) — bun.lock مُعاد توليده (47 سطرًا محذوفة، صفر تغيير مصدري) — 9/9 بوابات على استنساخ جديد (331/331)
- **(ي) 159 — قالب §6.3 مكتمل:** البندان 4+5 (دليل تفسيري + أسئلة شائعة) شُحنا على **24 مركزًا مملوءًا × لغتين** (7 عضلات + 7 معدات + 10 مجموعات أطعمة): دليل ~200–280 كلمة بإجابات ذرية لـ6 أسئلة/مركز بأسماء تمارين/أطعمة حقيقية من المكتبة — `hub-depth.ts` + 3 ملفات بيانات + `HubDepth.tsx` (بلا JS عميل) + ربط 6 صفحات — **صفر schema** (مصدر الحقيقة SEO-SCHEMA-REFERENCE: FAQPage retired 2026-05-07 وQAPage للمستخدمين) — عفاء موثق للمركزين الفارغين (cardio/none — تُعاد آليًا) — **+11 اختبارًا: 331/331** مع خرائط anti-fabrication تتحقق من انتماء كل اسم لمركزه
- **(ك) 157/158 — الإقران الرجعي + التوليد ثنائي اللغة:** 6 أزواج عالية الثقة مطبقة حيًا (12 منشورًا — hreflang + زر اللغة) وأزواج المراجعة الأربعة أُسقطت بقرار المالك «نكتفي بما تم» — صفّان بـpair_id + sharedBrief، كل لغة تنفذ P1→P5 كاملًا في نافذتها (AR 05:00 UTC · EN 22:00 UTC) — سلّم تدهور + مصافحة P5 + إصلاح روابط الـ85
- **(ل) 156→149:** درع وقت العرض (85 رابطًا + 3 مراسٍ + 3 CJK) · شبكة spoke→hub + lastmod · E-E-A-T YMYL · sameAs + فوتر الكيان · سايت مابز hreflang/IndexNow
- **آخر كوميت متحقق منه:** 954f37f (المرحلة 167 — EVO-4 كامل) — quality · parity · guard · Supabase Preview كلها success حيًا 2026-09-10 · **الدورة التعليمية الأولى شُغّلت حيًا workflow_dispatch (run 34415547011 ✓ success):** 9 مصادر (plans 3 · meal_plans 6) → 7 buckets + exemplar حي — المولد يقرأ المعرفة من التوليد التالي · **الإنتاج:** alkemos.com حي · **CI:** أخضر

## المفتوح الآن
- **EVO-5 (168) — مراقبة أول دورة تقييم أسبوعية:** workflow evo-weekly-eval.yml (خميس 21:00 UTC = جمعة 00:00 القاهرة) — إن لم تُشغَّل تلقائيًا تُفعّل بـAPI + كوميت لمس (قانون SCHEDULE HEALTH) — وworkflow_dispatch متاح لأي تشغيل يدوي — منحنى الجودة يظهر على /admin/evo-analytics — الكاش يتعبأ تلقائيًا من الأسئلة الشائعة (لا خطوات تفعيل)
- **EVO-4 (167) — مراقبة أول دورة تعلم أسبوعية:** workflow evo-weekly-learning.yml (أحد 21:00 UTC = إثنين 00:00 القاهرة) — أول تشغيل مجدول يثبت الدورة؛ ملاحظة: GitHub سبق أن ألغى تسجيل workflows المجدولة (SCHEDULE HEALTH LAW) — إن لم تُشغَّل تلقائيًا تُفعّل بـAPI + كوميت لمس (نمط process-ai-jobs الموثق) — وworkflow_dispatch متاح لأي تشغيل يدوي
- **مغلق ✓ — الإقران الرجعي (المقترح 3) بقرار المالك 2026-09-09:** «نكتفي بما تم» — الستة أزواج المطبقة حيًا تبقى، وأزواج المراجعة الأربعة في reviewPairs أُسقطت (تُبقى موثقة في pairs.json كسجل قرار لا قائمة عمل)
- **مراقبة أول دورتي GHA مقترنتين (متبقٍ اختياري — §12.16):** AR 05:00 UTC تنشئ الزوج (صفّان pair_id + sharedBrief) · EN 22:00 UTC تتبنى وتُشغل مصافحة P5 — التحقق: نسخة db-backup أو PostgREST (صفوف الطابور الجديدة + linked_post_id)
- **P1 بناء الكيان — المتبقي على المالك:** أول 10 تقييمات Trustpilot (0 حاليًا) · GBP · LinkedIn/YouTube/TikTok اختيارية (تُضاف لsocial.ts فينتشر تلقائيًا)
- **P1 فجوة محتوى (مؤجلة بقرار المالك):** كتلة كارديو لمكتبة التمارين — تعيد فتح /muscles/cardio و/equipment/none (وعندها يُشتق محتوى عمق المركزين تلقائيًا بزوال العفاء)
- **P1 SMTP/Brevo (مُهيأ — باقي فحص الدعوة الحية):** دعوة عميل حقيقية من التطبيق (E2E للمالك) + أول نشرة من لوحة Brevo
- **CSP (وفق خطة موثقة):** RO منشور 2026-09-05 13:34Z → الفرض مؤجل لـ2026-09-12 مع إضافة paypal+google-analytics لـconnect-src
- **AdSense lazy-load (مقصود):** الموقع قيد مراجعة AdSense — الوسم يبقى server-rendered حتى الموافقة
- **GSC إعادة إرسال — ✅ أضافها المالك 2026-09-08 (الخرائط السبع)** — Bing/Copilot مغطاة آليًا (IndexNow 202)

## بانتظار موافقة المالك
- **خطة تطوير Evo — ✅ القرارات الخمسة موثقة كلها §7 الوثيقة:** الذاكرة مجانية · لا «انسَ كل شيء» · يتعلم من كل شيء (مجهول الهوية) · D4 معتمد ببريد `evo@alkemos.com` إرسال فقط (**البريد مؤكد جاهزًا من المالك 2026-09-10**) — **EVO-1 منفذ (164) · EVO-2 منفذ (165) · EVO-3 منفذ + مُفعّل ومغلق E2E (166/166.1/166.2) · EVO-4 منفذ (167) · EVO-5 منفذ (168)** — التالي: EVO-6 (W6: API عام + ويدجت تضمين + white-label) **بابه مفتوح بأمر ملكي مستقل صريح** — كل بند W6 لا يُصمم تفصيليًا ولا يُنفذ إلا بأمر المالك
- Zod الموجات 2-3 (مسارات المستخدم/المدرب ثم الدفع وadmin وcron) — البنية جاهزة في `src/lib/validation/`
- لا عناصر مالية/بيانات معلقة

## ممنوعات نشطة
- `auth.users`: يدويًا فقط **باستثناء** DO-blocks محصّنة بمعالج استثناء تحت توجيه مالك صريح (نمط 0073/0074 المثبت) — التريغر الحالي قابل للإزالة بـDROP موثق داخل 0074 عند تهيئة SMTP
- مزودو AI: **OpenRouter + Groq + NVIDIA NIM فقط** — Gemini اتشالت بقرار المالك 2026-08-27؛ أي مزود رابع يحتاج أمر مالك صريح (قانون ai-provider.ts الوحيد)
- ممنوع تعديل ميجريشنز مطبَّقة — دايمًا ميجريشن جديدة بصيغة `YYYYMMDDHHMMSS_NNNN` (قانون INDEX.md §3)
- ممنوع أرقام متغيرة داخل README/DEVELOPER_GUIDE — مكانها الكود/INDEX.md (بوابة docs_audit تُفشل الدفع)
- ممنوع إحياء `PROGRESS.md`/`QA_CHECKLIST.md` في الجذر — اتجمدوا في archive/ بأمر Phase 115
- slug العمود لا يُمس أبدًا (قانون ثبات الروابط — المرحلة 121)

## ملخص جودة المرحلة (QA — Phase 168)
- **تغييرات المرحلة (EVO-5 — W5 المتبقي كامل):** ميجريشن `20260911120000_0081_evo5_cache_eval_stats.sql` (pg_trgm + evo_chat_cache/evo_call_stats/evo_eval_runs — RLS مفعّل صفر سياسات عميل posture 0080 موثق + دالة evo_cache_lookup بsearch_path مثبت + فهارس) · `evo-cache.ts` نقي (normalizeEvoQuestion حتمي: NFKC+تشكيل+أإآ/ة/ى/ؤ/ئ+أرقام عربية+ترقيم · isCacheEligibleMessage بوابة السياق-الصفر · chunkForSse بلا كسر كلمات) · حقن المسار: كاش fail-open بعد record-before-dispatch (الحصة تُستهلك كالعادة — الروابط حية للسؤال الجديد — الكاش يخدم SSE مقطع) + evo_call_stats صف لكل نداء (best-effort في after() — الردود الثابتة أزمة/بوابة/429 لا صفوف) + تخزين الإجابات المؤهلة ≥80 حرفًا بنجاح نموذجي (TTL 48س · ignoreDuplicates · تنظيف كسول 7 أيام) · `evo-system-prompt.ts` استخراج حرفي (مصدر واحد للمسار والتقييم) · `evo-eval.ts` + `evo-eval-runner.ts` + `scripts/ai-jobs-runner/evo-eval.mts` + `evo-weekly-eval.yml` (خميس 21:00 UTC) · `evo-export.ts` + زرا تصدير برأس الويدجت (بلا اعتماديات) · `/api/admin/evo-analytics` + `/admin/evo-analytics` + AdminShell — النسخ: /evo بطاقة + differences + README + AGENTS (قانون EVO-5 CACHE/EVAL/ANALYTICS) + DEVELOPER_GUIDE صف المسار — **+31 اختبارًا: 513/513**
- **حدود موثقة 168:** «دلالي» في الكاش = تطبيع نصي + trigram بلا embeddings (مزود رابع يحتاج أمر مالك) · الكاش يخدم فقط الطلبات بلا أي سياق شخصي — المشتركون وذوو الذاكرة ونوايا الخطط خارج الكاش دائمًا · إجابة الكاش بروابط محسوبة حيًا للسؤال الجديد لا لسؤال التخزين · eval يقيس baseline مجهول بلا RAG/تخصيص — منحنى ثابت المدخل عبر الزمن · القاضي فشل = خطأ ناعم بلا درجة، وصفر درجات = تشغيل أحمر · local fallback = success=false بالمزود (حصة الاحتياط باللوحة) · تصدير PDF عبر حوار الطباعة (لا مكتبة) — مانع النوافذ المنبثقة يظهر toast إرشادي · لوحة التحليلات قراءة فقط بجداول 0081 بلا سياسات عميل (service-role حصريًا) · workflow eval يحتاج مفاتيح AI (غير حتمي خلافًا لworkflow التعلم الحتمي)
- **(محفوظ من 167 — EVO-4):** ميجريشن 0080 evo_nutrition_patterns (bucket مقيد ثماني · UNIQUE(bucket,key) · صفر سياسات عميل انحراف موثق) — E1 تجميع نقي بلا LLM (classifyGoal بنفس regex المولد · normalizeFoodName عائلة evo-memory · شكلَا plans/meal_plans · أرضية صدق: bucket تحت 3 يُحجب · حارس هدف-مجهول) — E2 exemplar حتمي بحقول مهيكلة فقط — E3 تعلم الاستبدال (plan_swaps نوع + diff old/new هوية member-edit best-effort) — evo-learning-runner + evo-learning.mts + evo-weekly-learning.yml (أحد 21:00 UTC حتمي بلا مفاتيح AI) — حقن fail-open عبر كل المتصلين — **+33 اختبارًا**
- **حدود موثقة 167:** حقن المعرفة تجاهلي وليس حاكمًا — أرقام العميل الرسمية (computeNutritionTargets) تبقى المتحكمة والنص يصرّح بذلك · قاعدة فارغة/متعثرة = "" صفر تغيير سلوكي (fail-open) · plan_swaps بلا هوية أصناف (حد موثق) — هوية الاستبدال تتراكم فقط من استبدالات member-edit الجديدة · exemplar واحد latest (لا تعدد) · E1 يعيد البناء كليًا — أي إحصاء يدوي بالجدول يضيع بالتحديث (مقصود) · swap_volume آخر-أسبوع/إجمالي بنافذة 7 أيام ثابتة وقت التشغيل · workflow حتمي — لا نداء LLM في دورة التعلم (أرخص ممكن) · فشل soft مصدر واحد لا يفشل الدورة إن نجحت الكتابة لكنه يلوّن التشغيل أحمر (HONEST RUN COLOR)
- **(محفوظ من 166 — EVO-3):** `evo-coach.ts` نقي (resolveHistoryCap سقف 16/10 حسب المستوى · computeWeightDelta دلتا بين أقدم وأحدث قياس مع ثبات دون 0.2كغ · formatProgressForPrompt بلا قسم فارغ وبلا اختراع أرقام · needsFirstMeetingInterview بوابة المقابلة) · ربط api/ai/chat: سقف W2.4 (قص السلك عند 16 ثم تقطيع حسب المستوى) + W2.2 (إضافة التقدم إلى subscriberContext — إصلاح انجراف صامت) + W2.1 (بارامتر firstMeeting يحقن EVO_FIRST_MEETING_PROTOCOL ويتقدم على قاعدة سؤال واحد) · ميجريشن 0079 evo_followup_prefs (opt-in إلزامي افتراضي false · RLS: select مالك أو أدمن · insert مصادق بصفوفه · update مالك أو أدمن · delete أدمن فقط · فهرس جزئي where opted_in) · مرآة types.ts بعلاقة 1:1 · `evo-followup.ts` نقي (isFollowupDue بإيقاع 7 أيام · buildFollowupEmail عربي/إنجليزي بأرقام حقيقية فقط · ترميز HTML · تذييل opt-out بالمسار الحقيقي /profile · بلا replyTo — إرسال فقط D4) · `/api/evo/followup/dispatch` (بوابتان: العلم البيئي وإلا 404 ثم requireAdmin أو x-cron-secret · دفعات ≤50 · ختم النجاح فقط) — **+35 اختبارًا: 442/442** — INDEX.md 0079
- **تغييرات المرحلة (EVO-2):** ميجريشن `0078_evo_memory` (جدولان idempotent · RLS نمط chat_owner_or_coach · عداد بلا سياسات عميل · delete أدمن فقط D2) · أنواع types.ts (مرآة الجدولين) · `evo-memory.ts` نقي client-safe (normalizeForMemory بالأرقام العربية · isDuplicateMemoryFact تشابه نصي بحارس طول 8 · validateMemoryFacts ≤5/300 حرف بلا رمي · sanitizeStoredFacts · buildEvoMemoryPrompt بقائمة رفض PII + عقد JSON فقط · capTranscriptForExtraction · formatEvoMemoryForPrompt أعلى 15 بلا قسم فارغ · shouldExtractMemory) · ربط `api/ai/chat`: حقن الذاكرة لكل المسجلين (D1) + `recordMemoryProgress` داخل `after()` (عدّاد → استخلاص كل 10 → إدراج → تصفير — كل فشل warn فقط) — النسخ: صف ذاكرة /evo صار ✓ بحساب مجاني + صف جديد لاسترجاع السجل الكامل (D1 حقيقي والـPhase 69 لم يُمس) — **+22 اختبارًا: 407/407** · INDEX.md خريطة 0078
- **حدود موثقة 166:** المقابلة مدفوعة بالبرومبت — الحصة تُحتسب عند نية الخطة الأولى وإعادة الطلب الصريح تعيد الاحتساب (سلوك قائم موثق) · فشل مستلم واحد لا يجهض الدفعة (يعيد المحاولة الدورة القادمة) · إن فشل ختم last_sent_at بعد إرسال ناجح قد تتكرر رسالة الدورة القادمة (مسجل — غير ضار) · لا cron مجدول حتى التفعيل (المسار يدوي بأمر مالك) · لا واجهة opt-in قبل التفعيل (لا وعد بلا تنفيذ)
- **(محفوظ من 164):** دروع YMYL (evo-safety دقة عالية — قبل الحصص، الأزمة لا تُحسب) · شخصية المدرب في buildSystemPrompt · 0077 evo_feedback append-only + /api/ai/feedback (service-role · 20/د/IP · فشل=200 saved:false) · أزرار 👍/👎 بالويدجت (+18 اختبارًا)
- **حدود موثقة 164:** الأزرار لا تُميّز SSE الجزئي من النهائي (تقييم أثناء البث نادر وغير ضار — الرد النهائي هو المسجل) · فقاعات 429/أخطاء مستثناة من التقييم بمطابقة بادئة (⏰/عذراً) · الاستخلاص الآلي للذاكرة مؤجل لEVO-2 بالقرار المالك · بريد المتابعة إرسال فقط بلا استقبال (D4) والتفعيل بعد فحص SMTP الحي
- **(محفوظ من 162):** `blog-pipeline-dispatch.ts` (usableCoachTopic + fail-open · 8s) · route `/api/ai/jobs` إيصال · +15 اختبار (367/367) — ADOPT/JOIN يتجاهلان تجاوز الموضوع · المولد الأحادي احتياط — لا مسار ثالث
- **(محفوظ من 161):** درس مؤسسي: نموذج جديد = نداء حي إلزامي (الكتالوج ≠ التوفر) · عقد المهلة 480000 = 120s×4 · تهريب أحرف التحكم في parseJSON — التحقيقات من annotations عامة + PostgREST
- **ربط/تكرار (تحقق 161.5):** التكرار الموثق مصدره manual بنشر فوري لا المولد · الأزواج الستة المقترنة (158) مرتبطة حيًا منذ 2026-09-09T00:59Z — منشور 63 = sitemap-blog 63
- **البوابات:** bun install --frozen-lockfile ✓ · next build ✓ · tsc 0 · eslint 0/0 · vitest **513/513** (+31 EVO-5: 482 سابقًا = +33 EVO-4 · +35 EVO-3 · +7 166.1) · docs_audit · docs_parity · migration_audit --ci · stale-refs · ui-wiring
- **(محفوظ من 160):** أي تغيير تبعيات = bun install + كوميت bun.lock بنفس الفاز · **(محفوظ من 159):** النص المرئي هو أصل GEO · (محفوظ من 157/158): +22 اختبار اقتران · درع 156

## خريطة مصادر الحقيقة (ممنوع الوثوق برقم من غير مصدره)

| السؤال | المصدر الوحيد |
|---|---|
| شكل جداول/علاقات DB | `src/lib/supabase/types.ts` + التحقق الحي PostgREST |
| الأسعار والباقات | `src/lib/memberships.ts` |
| حل مستوى العضوية | `src/lib/membership-tier.ts` (Phase 141) |
| حدود التحقق للطلبات | `src/lib/validation/schemas.ts` (A-7) |
| الميجريشنز والترقيم | `supabase/migrations/INDEX.md` |
| الحالة الراهنة | الملف ده (STATE.md) — المصدر الوحيد من Phase 115 |
| خطة SEO/GEO وسجل تنفيذها | `docs/SEO-GEO-MASTER-PLAN.md` §12 |
| التاريخ الكامل | `worklog.md` + `archive/` |
| قوانين التشغيل | `AGENTS.md` |

## بروتوكول فتح الجلسة (النص الإلزامي الكامل: AGENTS.md §3.6)

1. اقرأ `STATE.md` (30 ثانية — الحقيقة الرسمية الوحيدة)
2. `git fetch origin --quiet` → آخر 3 مدخلات `worklog.md` + آخر 5 كوميتات
3. ممنوع الوثوق بأي رقم في أي وثيقة — خد منها من خريطة المصادر أعلاه
4. قانون البقاء: مساحة العمل بتتمسح في أي لحظة → أي معرفة عايزها تعيش = **commit & push في نفس الجلسة**
